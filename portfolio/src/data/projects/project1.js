const project1 = {
    "id": 1,
    "slug": "dynamic-eqs",
    "title": {
        "ko": "Dynamic EQS: RL 모델을 통한 EQS 가중치 동적 최적화 시스템",
        "en": "Dynamic EQS: Dynamic Optimization System for EQS Weights via RL Models"
    },
    "description": {
        "ko": "Schola와 Ray RLlib을 활용한 AWS 클라우드 기반 멀티 에이전트 병렬 강화학습 파이프라인 구축",
        "en": "Building an AWS Cloud-based Multi-Agent Parallel Reinforcement Learning Pipeline using Schola and Ray RLlib"
    },
    "image": "/images/project1/archi.png",
    "gif": "/gifs/project1/project1_battle.gif",
    "detailImage": "/gifs/project1/project1_title.gif",
    "period": "2025.08 - 2026.03",
    "team": {
        "ko": "1명",
        "en": "1 person"
    },
    "role": {
        "ko": "메인 프로그래머",
        "en": "Main Programmer"
    },
    "tags": [
        "Unreal Engine 5",
        "Reinforcement Learning",
        "EQS",
        "Ray RLlib",
        "AWS"
    ],
    "github": "https://github.com/yoosunghong/Dynamic-EQS",
    "highlights": {
        "ko": [
            "UE5 EQS 후보 위치 평가를 강화학습 정책의 7차원 연속 액션으로 제어",
            "MAPPO, centralized critic, self/cross attention을 사용한 팀 기반 협동 전략 학습",
            "Schola gRPC와 RLlib 사이에 재사용 가능한 RL-EQS 미들 레이어 구현",
            "AWS EC2 기반 병렬 학습 환경과 live evaluation 파이프라인 구축"
        ],
        "en": [
            "Controlled UE5 EQS candidate scoring with a 7-dimensional continuous RL action space",
            "Trained team cooperation with MAPPO, centralized critics, and self/cross attention",
            "Built a reusable RL-EQS middleware layer between Schola gRPC and RLlib",
            "Set up AWS EC2 parallel training and live evaluation pipelines"
        ]
    },
    "content": {
        "ko": "<hr class=\"project-divider\" />\n<h3>개요 (Overview)</h3>\n<p>본 프로젝트는 강화학습을 통해 UE5 에이전트의 EQS(Environment Query System) 가중치를 자동 최적화하는 Dynamic EQS 플러그인을 설계·구현하고, 5v5 팀 기반 거점 점령전 환경에서 MAPPO 학습을 통해 검증했습니다. 이를 통해 에이전트의 공간 탐색 및 이동은 EQS 시스템이 담당하고, 최적 위치 산정은 강화학습 모델이 담당하는 하이브리드 AI 시스템을 구현했습니다.</p>\n<p>학습 과정에서 팀 기반 협동 행동 유도를 위해 협동 보상 스칼라 설계 대신 아키텍처 차원의 어텐션 메커니즘을 도입했습니다. 이를 통해 각 에이전트가 동료 에이전트를 인지하고 중요 상태 정보를 선택적으로 식별할 수 있도록 하여 협동 전략이 자연스럽게 발현되도록 설계했습니다.</p>\n<p>이러한 시스템 전반은 Schola(gRPC) 위에 RL-EQS 통합 미들 레이어를 플러그인으로 추상화하여 다른 UE5 프로젝트에서도 재사용 가능한 구조로 설계했으며, AWS 기반 병렬 학습 파이프라인 구축까지 전 과정을 1인으로 구현했습니다.</figure></div>\n<hr class=\"project-divider\" />\n<h3>시스템 아키텍처 (System Architecture)</h3>\n<figure class=\"project-media\"><img src=\"/images/project1/archi.png\" alt=\"Fig 1. 시스템 아키텍처 및 계층적 포지셔닝 워크플로우\" loading=\"lazy\" /><figcaption>Fig 1. 시스템 아키텍처 및 계층적 포지셔닝 워크플로우</figcaption></figure>\n<hr class=\"project-divider\" />\n<h3>기술 스택 (Tech Stack)</h3>\n<figure class=\"project-media\"><img src=\"/images/project1/teckstack.png\" alt=\"\" loading=\"lazy\" /></figure>\n<hr class=\"project-divider\" />\n<h3>훈련 환경 (Training Environment)</h3>\n<p><strong>5 vs 5 팀 기반 거점 점령전</strong>으로, 맵 상에 5개의 거점(Capture Point)이 배치됩니다. 더 많은 거점을 점령·유지하여 목표 점수를 먼저 달성하는 팀이 승리합니다.</p>\n<hr class=\"project-divider\" />\n<h4>클래스 역할 설계</h4>\n<p>각 에이전트는 매 에피소드마다 <strong>Strike / Vanguard / Support</strong> 클래스를 부여받습니다. 클래스마다 요구되는 포지셔닝 전략이 근본적으로 다르므로, 각 클래스에 <strong>독립된 정책 네트워크</strong>를 할당하고, 역할에 특화된 보상 함수를 적용했습니다.</p>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/gifs/project1/project1_strike.gif\" alt=\"\" loading=\"lazy\" /></figure><figure class=\"project-media\"><img src=\"/gifs/project1/project1_vanguard.gif\" alt=\"\" loading=\"lazy\" /></figure><figure class=\"project-media\"><img src=\"/gifs/project1/project1_support.gif\" alt=\"\" loading=\"lazy\" /></figure></div>\n<div class=\"project-table-wrap\"><table><thead><tr><th>클래스</th><th>역할</th><th>핵심 포지셔닝 목표</th></tr></thead><tbody><tr><td><strong>Strike</strong></td><td>원거리 딜러</td><td>적 거점 접근 + 사거리 유지</td></tr><tr><td><strong>Vanguard</strong></td><td>근접 탱커</td><td>전열 유지 + 근접 교전</td></tr><tr><td><strong>Support</strong></td><td>후방 힐러</td><td>부상 아군 추적 + 후방 포지션</td></tr></tbody></table></div>\n<hr class=\"project-divider\" />\n<h4>커리큘럼 티어 시스템</h4>\n<p><strong>3단계 난이도 티어</strong>(1=Basic, 2=Standard, 3=Aggressive)를 도입하여 초중반은 RL 에이전트의 기본 게임 규칙 학습을 위해 EQS 가중치가 하드코딩된 스크립트 AI를 상대로 학습하고, 이후에는 ONNX 로컬 추론 에이전트와 스크립트 AI를 <strong>3:2 비율로 믹싱</strong>한 대결을 통해 학습 성능을 극대화하고 <strong>과적합 문제, 가위바위보 문제</strong>를 해결하려 하였습니다.</p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>티어</th><th>승격 임계값</th></tr></thead><tbody><tr><td>Tier 1(Script)</td><td>승률 25% 이하</td></tr><tr><td>Tier 2(Script)</td><td>승률 40% 이하</td></tr><tr><td>Tier 3(Self-Play)</td><td>승률 40% 초과</td></tr></tbody></table></div>\n<p>티어 정보는 <code>scripted_ai_config.json</code>에 기록되며, UE5에서 다음 에피소드 리셋 시 자동으로 적용합니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<hr class=\"project-divider\" />\n<h4>관측 공간</h4>\n<p>218-dim 엔티티 중심(Entity-Centric) 벡터입니다. 아군·적·거점을 고정 크기 슬롯 토큰으로 인코딩하고, 패딩 마스크를 별도로 제공해 중앙 Attention이 유효 엔티티만 처리하도록 합니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h5><strong>에이전트 입력 상태(State) 구성표</strong></h5>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Index</th><th>Dim</th><th>토큰 내용</th><th>정규화 및 상세 설명</th></tr></thead><tbody><tr><td><strong>[0 : 7]</strong></td><td>7</td><td>자신 토큰</td><td>위치/7500(3) + 속도/600(3) + 체력(1)</td></tr><tr><td><strong>[7 : 71]</strong></td><td>64</td><td>아군 토큰 (8×8)</td><td>상대 위치/8000(3) + 체력(1) + 생존 여부(1) + 클래스 원-핫(3)</td></tr><tr><td><strong>[71 : 135]</strong></td><td>64</td><td>적 토큰 (8×8)</td><td>상대 위치/8000(3) + 체력(1) + 가시성(1) + 클래스 원-핫(3)</td></tr><tr><td><strong>[135 : 191]</strong></td><td>56</td><td>거점 토큰 (8×7)</td><td>상대 위치/15000(2) + 높이/1000(1) + 점유(1) + 점령 진행도(1) + 할당 여부(1) + 전략적 가치(1)</td></tr><tr><td><strong>[191 : 199]</strong></td><td>8</td><td>아군 마스크</td><td>0=유효, 1=패딩</td></tr><tr><td><strong>[199 : 207]</strong></td><td>8</td><td>적 마스크</td><td>0=유효, 1=패딩</td></tr><tr><td><strong>[207 : 215]</strong></td><td>8</td><td>거점 마스크</td><td>0=유효, 1=패딩</td></tr><tr><td><strong>[215 : 218]</strong></td><td>3</td><td>클래스 원-핫</td><td>[strike, vanguard, support]</td></tr><tr><td><strong>TOTAL</strong></td><td><strong>218</strong></td><td></td><td></td></tr></tbody></table></div>\n<blockquote><strong>마스크 처리:</strong> Python 정책의 <code>_safe_mask()</code>는 모든 슬롯이 패딩일 때 슬롯 0을 강제 언마스크하여 중앙 Attention의 NaN을 방지합니다. 마스크 임계값은 <code>&gt; 0.5</code> (float 비교)로, <code>0.0=유효 / 1.0=패딩</code> 의미론을 보존합니다.</blockquote>\n<hr class=\"project-divider\" />\n<h4>액션 공간</h4>\n<p>정책 네트워크의 출력은 <strong>7-dim Continuous Box Action</strong>입니다. 각 차원은 EQS 쿼리의 개별 테스트에 대응하는 가중치 파라미터로, EQS가 48개 후보 위치를 평가할 때 각 테스트의 기여도를 동적으로 조절합니다.</p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Index</th><th>파라미터 이름</th><th>범위</th><th>역할 설명</th></tr></thead><tbody><tr><td><strong>[0]</strong></td><td><code>EnemyObjectiveProximity</code></td><td>[-1, 1]</td><td>적 거점 방향 이동 욕구 (양수: 접근, 음수: 회피)</td></tr><tr><td><strong>[1]</strong></td><td><code>AllyObjectiveProximity</code></td><td>[-1, 1]</td><td>아군 거점 방어 욕구 (양수: 귀환, 음수: 전진)</td></tr><tr><td><strong>[2]</strong></td><td><code>CoverDensity</code></td><td>[-1, 1]</td><td>엄폐물 밀집 지역 선호도 (양수: 엄폐 우선, 음수: 개방 지형 선호)</td></tr><tr><td><strong>[3]</strong></td><td><code>EnemyVisibility</code></td><td>[-1, 1]</td><td>적 가시선 유지 욕구 (양수: 시야 확보, 음수: 은폐)</td></tr><tr><td><strong>[4]</strong></td><td><code>AllyProximity</code></td><td>[-1, 1]</td><td>아군 근접 유지 욕구 (양수: 뭉침, 음수: 분산)</td></tr><tr><td><strong>[5]</strong></td><td><code>CombatRange</code></td><td>[-1, 1]</td><td>선호 교전 거리 조절 (양수: 원거리 유지, 음수: 근접 추구)</td></tr><tr><td><strong>[6]</strong></td><td><code>AssignedBaseProximity</code></td><td>[-1, 1]</td><td>할당 거점 방향 인력 (양수: 할당 거점 접근, 음수: 이탈)</td></tr><tr><td><strong>TOTAL</strong></td><td><strong>7-dim</strong></td><td><strong>[-1, 1]</strong></td><td><code>FDEEQSWeightParameters</code></td></tr></tbody></table></div>\n<p>정책 네트워크의 출력 레이어는 <code>tanh</code> 활성화를 사용하여 각 가중치를 자연스럽게 [-1, 1] 범위로 제한합니다. 전달받은 7개 가중치는 프레임워크를 통해 EQS 쿼리 파라미터로 주입되고, EQS는 이를 바탕으로 48개 후보 위치에 대한 종합 점수를 산출하여 최적 이동 목표를 결정합니다.</p>\n<p>※ 향후에는 Continous 대신 Discrete를 사용하여 액션 스페이스 최적화를 고려할 예정입니다.</p>\n<hr class=\"project-divider\" />\n<h4>보상 구조</h4>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>S T R I K E</h4>\n<p>목표는 원거리에서 높은 데미지를 유지하며 거점을 점령하는 것입니다. 적 거점까지의 거리 감소분에 비례한 접근 보상을 매 스텝 부여하고, 거점 반경 내 진입 시 추가 존재 보너스를 부여합니다. 적과 너무 가까운 경우 원거리 역할 이탈 페널티가 부과됩니다.</p>\n<figure class=\"project-code-block\"><figcaption>Strike reward (per step)</figcaption><pre><code class=\"language-cpp\">reward = 0\r\nif distance_to_target_base decreased:\r\n    reward += approach_reward * delta_distance\r\nif agent inside target_base radius:\r\n    reward += zone_presence_bonus\r\nif base just captured:\r\n    activate momentum_bonus for PostCaptureMomentumDuration steps\r\n    reward += capture_bonus\r\nif momentum active:\r\n    reward += momentum_bonus  # encourages moving to next base\r\nif distance_to_enemy &lt; MinCombatRange:\r\n    reward -= too_close_enemy_penalty  # ranged: maintain combat distance</code></pre></figure>\n<hr class=\"project-divider\" />\n<h4>V A N G U A R D</h4>\n<p>목표는 전열에서 근접 전투를 수행하며 거점을 점령하는 것입니다. 적 거점 접근 및 점령 방식은 Strike와 동일하게 적용됩니다. 거점 내에서 근접 사거리에 적이 있을 때 추가 근접 보너스가 지급되어, 전선을 유지하며 적과 밀착하는 행동을 강화합니다.</p>\n<figure class=\"project-code-block\"><figcaption>Vanguard reward (per step)</figcaption><pre><code class=\"language-cpp\">reward = 0\r\nif distance_to_target_base decreased:\r\n    reward += approach_reward * delta_distance\r\nif agent inside target_base radius:\r\n    reward += zone_presence_bonus\r\n    if enemy within melee range:\r\n        reward += melee_range_bonus  # frontline tank: reward close engagement\r\nif base just captured:\r\n    activate momentum_bonus for PostCaptureMomentumDuration steps\r\n    reward += capture_bonus\r\nif momentum active:\r\n    reward += momentum_bonus</code></pre></figure>\n<hr class=\"project-divider\" />\n<h4>S U P P O R T</h4>\n<p>목표는 데미지가 심한 아군을 추적하고 치유하며 후방 포지션을 유지하는 것입니다. 매 스텝 부상 아군 탐색을 수행하되, 잦은 타겟 전환으로 인한 진동 행동을 막기 위해 5스텝 캐시를 적용합니다. Support를 제외한 아군 뒤편에 위치하면 후방 포지셔닝 보너스를 받습니다.</p>\n<figure class=\"project-code-block\"><figcaption>Support reward (per step)</figcaption><pre><code class=\"language-cpp\">reward = 0\r\nif staleness_counter &gt;= 5 or no cached_target:\r\n    cached_target = ally with lowest health\r\n    staleness_counter = 0\r\nelse:\r\n    staleness_counter++\r\n\r\nif distance_to_cached_target decreased:\r\n    reward += approach_reward * delta_distance\r\nif heal applied to ally:\r\n    reward += heal_reward * heal_amount\r\nif agent positioned behind cached_target (rear arc):\r\n    reward += rear_positioning_bonus\r\nif cached_target.health &lt; threshold and agent attempted kill:\r\n    reward -= role_deviation_penalty</code></pre></figure>\n<hr class=\"project-divider\" />\n<h4>팀 보상 믹싱 (Team Reward Mixing — MAPPO Cooperative Signal)</h4>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p>팀 기반의 환경에서 협동성을 강화하기 위해 개인 보상과 팀 평균 보상을 혼합하는 방식을 도입했습니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<div class=\"project-math\">\\text{final\\_reward} = (1 - \\alpha) \\times r_{\\text{individual}} + \\alpha \\times r_{\\text{team\\_avg}}</div>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<div class=\"project-table-wrap\"><table><thead><tr><th>파라미터</th><th>값</th><th>설계 의도</th></tr></thead><tbody><tr><td><code>TeamRewardMixingRatio</code> (α)</td><td><strong>0.2</strong></td><td>개인 역할 최적화를 주 신호로 유지하되, 팀 협동 신호를 20% 혼합하여 과도한 이기적 행동 방지</td></tr></tbody></table></div>\n<p>초반 학습에서 각 역할의 기초 행동(포지셔닝, 사거리 유지 등)을 먼저 수렴시킨 후 팀 협동 행동이 학습되도록 α = 0.2로 설정하였습니다.</p>\n<p>팀 평균은 자신을 제외한 동일 팀 에이전트들의 <code>LastIndividualStepReward</code> 평균값이며, 팀원이 1명일 경우 믹싱은 자동으로 비활성화됩니다. 이 계산은 <code>DERewardSubsystem.cpp</code>의 <code>CalculateStepReward()</code> 함수에서 역할별 보상 산출 직후 매 스텝 적용됩니다.</p>\n<hr class=\"project-divider\" />\n<h4>UE5 → Python 보상 파이프라인 (Reward Pipeline)</h4>\n<p>보상은 <strong>C++ (UE5)</strong> 에서 계산되고 <strong>Python (RLlib)</strong> 에서 정규화된 후 PPO 업데이트에 사용됩니다.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/rewardpipeline.png\" alt=\"Fig 2. 보상 파이프라인\" loading=\"lazy\" /><figcaption>Fig 2. 보상 파이프라인</figcaption></figure>\n<p><strong>단계별 흐름:</strong></p>\n<ol><li><strong>C++ 보상 계산:</strong> <code>DERewardSubsystem</code>이 매 스텝 역할별 보상(접근, 거점, 전투 등)을 계산하고, 팀 보상 믹싱(80:20)을 적용한 뒤 <code>RewardScale</code>/<code>Clamp</code>으로 1차 정규화합니다.</li><li><strong>gRPC 전송:</strong> Schola 플러그인이 보상을 gRPC를 통해 Python 환경(<code>DEEntityCentricEnv</code>)으로 전달합니다.</li><li><strong>Python 정규화:</strong> <code>process_reward()</code>가 <code>reward_scale=0.01</code>로 스케일링하고 <code>±5.0</code>으로 클리핑하여 PPO 학습 안정성을 확보합니다.</li><li><strong>Dual Critic 평가:</strong> <code>EntityCentricRLlibModel</code>의 value function이 학습 가능한 혼합 계수(α, sigmoid 초기값 0.5)를 사용하여 V_local(226-dim 에이전트 관찰 기반 attention critic)과 V_central(71-dim 글로벌 팀 상태 기반 MLP critic)을 결합합니다. 세 역할 정책이 centralized critic을 공유합니다.</li></ol>\n<hr class=\"project-divider\" />\n<h3>주요 기능 (Key Features)</h3>\n<h4>1. Dynamic-EQS Plugin</h4>\n<p><strong>DynamicEQS</strong>는 Schola 플러그인을 기반으로 하는 <strong>재사용 가능한 UE5 전용 RL-EQS 통합 레이어</strong>입니다. DynamicEQS를 사용하는 게임 프로젝트는 Schola의 저수준 gRPC/ONNX 처리를 직접 다루지 않고 EQS 특화 추상 클래스만 상속하면 됩니다.</p>\n<p>DynamicEQS의 주요 클래스는 4가지로, <strong>환경</strong>, <strong>에이전트</strong>, <strong>액션</strong>, <strong>관찰</strong>을 담당합니다. Schola와 게임 프로젝트의 미들 계층에서 EQS 가중치를 설정하고 정책 네트워크와 매핑해주는 역할을 합니다.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/pluginarchi.png\" alt=\"Fig 3. 플러그인 계층 구조. 붉은 박스가 실제 구현한 부분입니다\" loading=\"lazy\" /><figcaption>Fig 3. 플러그인 계층 구조. 붉은 박스가 실제 구현한 부분입니다</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/flow3.png\" alt=\"Fig 4. 학습/추론 런타임 데이터 플로우\" loading=\"lazy\" /><figcaption>Fig 4. 학습/추론 런타임 데이터 플로우</figcaption></figure>\n<hr class=\"project-divider\" />\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>EQS 가중치 주입</h4>\n<p>정책 네트워크의 출력(7-dim Box action)이 실제 EQS 쿼리 파라미터로 주입되는 전체 흐름은 다음과 같습니다.</p>\n<p><strong>에디터 설정</strong></p>\n<ol><li>에이전트에 <code>UDynamicEQSExecutor</code> 컴포넌트를 추가합니다.</li><li><code>WeightParamNames</code> 배열에 EQS Query의 각 테스트에서 사용하는 파라미터 이름을 순서대로 입력합니다 (예: <code>EnemyObjectiveProximity</code>, <code>AllyObjectiveProximity</code>, …).</li><li><code>UDEScholaAgent</code> 컴포넌트에서 <code>Actuator</code>로 <code>UDETacticalParameterActuator</code>를 지정합니다.</li></ol>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/eqs1.png\" alt=\"Fig 5. DynamicEQSExecutor 컴포넌트\" loading=\"lazy\" /><figcaption>Fig 5. DynamicEQSExecutor 컴포넌트</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/eqs2.png\" alt=\"Fig 6. EQS 에셋 구성\" loading=\"lazy\" /><figcaption>Fig 6. EQS 에셋 구성</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/eqsdebug1.png\" alt=\"Fig 7. EQS Debugging 1\" loading=\"lazy\" /><figcaption>Fig 7. EQS Debugging 1</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/eqsdebug2.png\" alt=\"Fig 8. EQS Debugging 2\" loading=\"lazy\" /><figcaption>Fig 8. EQS Debugging 2</figcaption></figure></div>\n<figure class=\"project-code-block\"><figcaption>EQS Parameter Injection</figcaption><pre><code class=\"language-cpp\">// DynamicEQSExecutor.cpp — EQS 파라미터 주입\r\nvoid UDynamicEQSExecutor::ApplyWeightsToRequest(FEnvQueryRequest&amp; Request) const\r\n{\r\n    for (int32 i = 0; i &lt; Weights.Num(); ++i)\r\n    {\r\n        const FName ParamName = (WeightParamNames.IsValidIndex(i) &amp;&amp; !WeightParamNames[i].IsNone())\r\n            ? WeightParamNames[i]\r\n            : FName(*FString::Printf(TEXT(\"Weight%d\"), i));\r\n        Request.SetFloatParam(ParamName, Weights[i]);\r\n    }\r\n}</code></pre></figure>\n<h4>평가 모드 (Evaluation Mode)</h4>\n<p>학습 중 저장된 체크포인트의 성능을 ScriptedAI 대전을 통해 정량적으로 검증하는 <strong>Live Evaluation</strong> 파이프라인(<code>eval_live.py</code>)입니다.</p>\n<p>UE5에 2개의 서브 환경을 동시 연결하여 두 개의 체크포인트(<code>best</code>, <code>latest</code>)를 병렬로 평가합니다. 에피소드 결과를 <code>win / loss / draw / timeout</code>으로 분류하고, 50회 에피소드 기준 승률을 <code>eval_results_&lt;timestamp&gt;.json</code>으로 저장합니다.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/eval.png\" alt=\"Fig 9. 모델 평가 결과\" loading=\"lazy\" /><figcaption>Fig 9. 모델 평가 결과</figcaption></figure>\n<hr class=\"project-divider\" />\n<hr class=\"project-divider\" />\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>FInstancedStruct를 활용한 게임 로직 디커플링</h4>\n<p>플러그인이 게임 전용 타입(<code>AssignedBaseIndex</code> 등)을 직접 멤버로 갖지 않도록, <code>FInstancedStruct</code>로 외부 파라미터를 불투명하게 보관합니다.</p>\n<p>플러그인이 게임 모듈에 대한 <strong>컴파일 타임 의존성</strong>을 가지면 다른 프로젝트에서 재사용이 불가능해지므로, 런타임 타입 정보를 보존하면서 모듈 간 의존성을 끊는 방법이 필요했습니다. <code>void*</code>는 타입 안전성이 없고, 인터페이스 패턴(<code>IExternalContext</code>)은 게임 측에 구현을 강제하여 플러그인 자체의 독립성을 훼손합니다. UE5의 <code>FInstancedStruct</code>는 USTRUCT 메타데이터를 보존하면서 불투명 저장이 가능해 이 요구사항에 부합했습니다.</p>\n<figure class=\"project-code-block\"><figcaption>DynamicEQSAgentComponent.h</figcaption><pre><code class=\"language-cpp\">UPROPERTY(BlueprintReadWrite)\r\nFInstancedStruct ExternalParams;  // 어떤 USTRUCT도 저장 가능\r\n\r\n// DETacticalParameterActuator.cpp — 사용 시점에 게임 타입으로 캐스팅\r\nconst FDEAgentExternalContext* Ctx =\r\n    AgentComponent-&gt;ExternalParams.GetPtr&lt;FDEAgentExternalContext&gt;();\r\nif (Ctx)\r\n    Weights.AssignedBaseProximity = ComputeBaseProximityWeight(Ctx-&gt;AssignedBaseIndex);</code></pre></figure>\n<p>이 패턴 덕분에 플러그인은 게임 헤더를 전혀 포함하지 않으며, 다른 프로젝트에서 그대로 재사용할 수 있습니다.</p>\n<hr class=\"project-divider\" />\n<h4>2. MAPPO (Multi-Agent PPO)</h4>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p>MAPPO(Multi-Agent PPO)를 채택하여 Continuous 액션 스페이스를 처리하고, Intra-Set Self Attention + Cross Attention을 통해 중앙 집중식으로 신뢰 할당 문제(Credit Assignment Problem)를 해결하고자 했습니다.</p>\n<p><strong>중앙집중식 Critic (Centralized Critic)</strong> — 71-dim 전역 팀 상태(아군 5명 위치·체력·전략 + 적 5명 위치·신뢰도 + 맵 상태)를 입력받아 팀 전체의 가치를 추정합니다. 개별 에이전트 관측만으로는 알 수 없는 팀-레벨 정보(아군 분포, 전체 거점 점령 상황 등)를 Critic이 직접 참조하므로 Advantage 추정의 분산이 감소합니다.</p>\n<hr class=\"project-divider\" />\n<h4>Dual Value Estimation</h4>\n<p>각 <code>EntityCentricRLlibModel</code>은 로컬 Critic(<code>V_local</code>, 218-dim 에이전트 관측)과 중앙 Critic(<code>V_central</code>, 71-dim 전역 상태)을 학습 가능한 혼합 계수 α로 결합합니다.</p>\n<p>$<span class=\"project-inline-math\">V = \\alpha \\cdot V_{\\text{local}}(\\text{agent\\_obs}[0:218]) + (1 - \\alpha) \\cdot V_{\\text{central}}(\\text{global\\_state}[218:289])</span>$</p>\n<p>α는 <code>sigmoid(_value_mix_logit)</code>로 초기화되며(초기값 0.5), 학습을 통해 각 역할에 최적인 로컬/전역 비중을 자동으로 결정합니다.</p>\n<div class=\"project-spacer\" style=\"--space:50px\"></div>\n<hr class=\"project-divider\" />\n<h4>Self-Attention의 역할</h4>\n<p>Actor의 인코더에서 아군·적·거점 각 엔티티 그룹에 Intra-Set Self-Attention을 적용합니다(Zambaldi et al., 2018). 엔티티 토큰들이 서로를 참조하여 <strong>\"슬롯 3과 슬롯 5가 같은 거점 근처에 집결\"</strong> 같은 집합 내 공간 관계를 학습합니다. 이 문맥화된 표현이 이후 Cross-Attention의 입력으로 사용됩니다.</p>\n<div class=\"project-spacer\" style=\"--space:50px\"></div>\n<hr class=\"project-divider\" />\n<h4>Cross-Attention의 역할</h4>\n<p>Self Token(자신 관측 7-dim의 임베딩)이 Query가 되고, Self-Attention을 거친 아군·적·거점 토큰이 Key/Value가 됩니다. Cross-Attention은 <strong>\"현재 나의 상태에서 각 엔티티가 얼마나 중요한가\"</strong> 를 가중합으로 집약하여 행동(EQS 가중치 7-dim)을 결정합니다.</p>\n<figure class=\"project-code-block\"><figcaption>train.py</figcaption><pre><code class=\"language-python\"># 역할별 독립 정책 라우팅\r\nSTRATEGY_POLICY_NAMES = {0: \"strike_policy\", 1: \"vanguard_policy\", 2: \"support_policy\"}\r\n\r\nconfig = config.multi_agent(\r\n    policies={\r\n        name: PolicySpec(config={\"model\": model_cfg})\r\n        for name in STRATEGY_POLICY_NAMES.values()\r\n    },\r\n    policy_mapping_fn=_policy_mapping_fn,  # agent_id → class → policy\r\n    count_steps_by=\"agent_steps\",\r\n)</code></pre></figure>\n<p>자세한 내용은 <strong>\"Problem 2: 엔티티 간 관계 정보 소실\"</strong> 섹션에서 다룹니다.</p>\n<hr class=\"project-divider\" />\n<h4>3. AWS 병렬 학습 환경 (AWS Parallel Training Environment)</h4>\n<p>로컬 단일 UE5 인스턴스의 한계를 넘어 클라우드 병렬 환경으로 확장하기 위해 AWS 기반 분산 학습 파이프라인을 구축했습니다. UE5를 Linux 바이너리로 패키징하고 S3에 적재, 학습 스크립트를 Docker 이미지로 EC2 인스턴스에서 실행하여 병렬 학습을 수행합니다.</p>\n<hr class=\"project-divider\" />\n<h4>전체 인프라 구성</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/aws_archi.png\" alt=\"Fig 10. AWS 병렬 학습 인프라 전체 구성도\" loading=\"lazy\" /><figcaption>Fig 10. AWS 병렬 학습 인프라 전체 구성도</figcaption></figure>\n<hr class=\"project-divider\" />\n<h4>인프라 구성</h4>\n<div class=\"project-table-wrap\"><table><thead><tr><th>컴포넌트</th><th>역할</th></tr></thead><tbody><tr><td><strong>Amazon ECR</strong></td><td>훈련 스크립트 Docker 이미지 레지스트리, S3로부터 UE5 바이너리 다운로드</td></tr><tr><td><strong>EC2 Ray 클러스터</strong></td><td>Ray head + env_runners 4개 동시 실행, GPU로 PPO 학습</td></tr><tr><td><strong>Amazon S3</strong></td><td>UE5 바이너리 저장, 체크포인트, 훈련 데이터 영구 저장 및 W&B 연동</td></tr><tr><td><strong>W&B</strong></td><td>이터레이션별 보상·승률·손실 지표 실시간 모니터링</td></tr><tr><td><strong>Terraform</strong></td><td>VPC·IAM·보안 그룹 프로비저닝</td></tr></tbody></table></div>\n<hr class=\"project-divider\" />\n<h4>클러스터 설계 의도</h4>\n<p>Head 노드(GPU)는 정책 gradient 업데이트를, Worker 노드(CPU Spot)는 UE5 헤드리스 인스턴스 구동 및 롤아웃 수집을 전담합니다. 롤아웃 수집은 중단·재시작이 가능한 stateless 작업이기 때문에 Worker는 Spot 인스턴스로 구성하여 비용을 절감했습니다.</p>\n<hr class=\"project-divider\" />\n<h4>학습 사이클 요약</h4>\n<ol><li>Docker 이미지 빌드 → ECR Push</li><li>Ray 클러스터 기동 (<code>ray up</code>) → 학습 스크립트 업로드</li><li>Worker 4개가 병렬로 UE5 롤아웃 수집 → <code>train_batch_size=4096</code> 배치 구성</li><li>Head GPU에서 PPO gradient 업데이트 → 10 이터레이션마다 S3 체크포인트 동기화</li><li>신규 최고 승률 달성 시 <code>best/</code> 경로에 즉시 추가 동기화</li></ol>\n<p><code>ray down</code> 시 모든 인스턴스가 즉시 종료되며, S3 체크포인트는 유지되어 재기동 시 이어서 학습할 수 있습니다.</p>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/aws_ec2.png\" alt=\"Fig 11. AWS EC2 인스턴스\" loading=\"lazy\" /><figcaption>Fig 11. AWS EC2 인스턴스</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/aws_ps.png\" alt=\"Fig 12. ps aux 커맨드로 확인한 4개의 PID(UE5 프로세스)\" loading=\"lazy\" /><figcaption>Fig 12. ps aux 커맨드로 확인한 4개의 PID(UE5 프로세스)</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/aws_s3.png\" alt=\"Fig 13. AWS S3\" loading=\"lazy\" /><figcaption>Fig 13. AWS S3</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/aws_log.png\" alt=\"Fig 14. CloudWatch Log\" loading=\"lazy\" /><figcaption>Fig 14. CloudWatch Log</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/wan_spec.png\" alt=\"Fig 15. WAN DB Spec\" loading=\"lazy\" /><figcaption>Fig 15. WAN DB Spec</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/wan_log.png\" alt=\"Fig 16. WAN DB Log\" loading=\"lazy\" /><figcaption>Fig 16. WAN DB Log</figcaption></figure></div>\n<hr class=\"project-divider\" />\n<h3>기술적 난제 및 해결 전략 (Problem Solving)</h3>\n<h4>Problem 1: 멀티 에이전트 강화학습 환경(Schola + RLlib)에서의 에이전트 개별 사망 처리 결함</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/problem1.png\" alt=\"Fig 17. 프리징 현상의 원인과 해결\" loading=\"lazy\" /><figcaption>Fig 17. 프리징 현상의 원인과 해결</figcaption></figure>\n<p><strong>에피소드 멈춤(Episode Freeze)</strong>: 특정 에이전트가 먼저 사망할 경우, RLlib은 해당 에이전트의 액션을 전송하지 않지만 Unreal Engine Schola는 모든 에이전트의 액션을 기다리며 대기 상태에 빠지는 통신 불일치가 발생했습니다.</p>\n<p><strong>부활 루프(Death-resurrection loop)</strong>: SAME_STEP 모드에서 사망한 에이전트가 유효한 상태 없이 즉시 리셋되어 다시 사망하는 무한 루프 현상 발생했습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Cause</h4>\n<p><strong>두 종료 시스템의 충돌</strong></p>\n<p>Schola 측(<code>AutoResetType::SAME_STEP</code>)과 Python 측(RLlib) 사이에 에피소드 종료 신호가 서로 모순되는 상태였습니다.</p>\n<ul><li><strong>Schola 측</strong>: 에이전트가 사망하면 <code>SAME_STEP</code> 정책에 의해 즉시 자동 리셋을 트리거했습니다.</li><li><strong>Python 측</strong>: RLlib은 혼합 궤적(서로 다른 에피소드의 데이터가 한 배치에 섞이는 것)이 생기지 않도록, 모든 에이전트의 종료 신호(<code>done</code>)를 억제하고 있었습니다.</li></ul>\n<p>결과적으로 사망한 에이전트는 Schola가 부활시키자마자 다시 사망하는 무한 루프에 빠지고, 그 루프가 Schola의 스텝 예산(step budget) 전체를 소비해버렸습니다. 생존한 에이전트들은 스텝 버짓이 고갈된 Schola의 멀티에이전트 동기화 장벽(step barrier)에 막혀 영원히 다음 액션을 받지 못하는 상태가 되었습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Goal</h4>\n<p>시차를 두고 발생하는 에이전트 사망(Staggered Death) 상황에서도 시스템 중단 없는 안정적인 학습 환경 구축</p>\n<ul><li>에이전트별 사망 시점이 달라도 전체 에피소드가 정상적으로 종료(__all__=True)되도록 보장.</li><li>사망한 에이전트의 관측값이나 보상이 학습 데이터에 오염(NaN 발생 등)을 일으키지 않도록 필터링 시스템 구현.</li></ul>\n<hr class=\"project-divider\" />\n<h4>Solution</h4>\n<p><strong>Python(통신 계층)과 C++(엔진 계층)의 이중 레이어 수정</strong></p>\n<p>Python (Schola Wrapper):</p>\n<ul><li>No-op Padding: 사망한 에이전트의 빈자리에 무효 액션(noop)을 삽입하여 Unreal이 항상 전체 에이전트의 액션을 수신하도록 보정.</li><li>Data Filtering: Unreal로부터 받은 응답 중, 이미 사망한 에이전트의 관측값/보상/정보를 필터링하여 RLlib에 전달.</li></ul>\n<p>C++ (Unreal Plugin):</p>\n<ul><li>Dead Agent Snapshot: Step() 실행 전 사망한 에이전트 상태를 기록하고, 실행 후 터미널 플래그(Terminal Flags)를 재복구하여 상태 덮어쓰기 및 부활 루프 방지.</li><li>Action Filter: 사망한 에이전트의 액션이 물리 엔진 및 로직에 영향을 주지 않도록 제외 처리.</li></ul>\n<hr class=\"project-divider\" />\n<h4>Result</h4>\n<ul><li>단위 테스트 통과: 총 10종의 Standalone 테스트(No-op 생성, 패딩 프로토콜 등) 100% 통과.</li></ul>\n<ul><li>학습 안정성 확보: 실제 Unreal 통합 환경에서 에피소드 정지 현상 해결 및 정상적인 에피소드 리셋 주기 확인.</li></ul>\n<ul><li>데이터 무결성: 보상 체계에서의 NaN 발생 및 Trajectory 누수 차단 확인.</li></ul>\n<ul><li>관련 문제 Schola 오픈소스에 PR 제출.</li></ul>\n<figure class=\"project-media\"><img src=\"/images/project1/pr.png\" alt=\"Fig 18. Pull Request\" loading=\"lazy\" /><figcaption>Fig 18. Pull Request</figcaption></figure>\n<hr class=\"project-divider\" />\n<h4>Problem 2: 엔티티 간 관계 정보 손실</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/problem2.png\" alt=\"Fig 19. 엔티티 관계 정보 손실과 어텐션 도입을 통한 해결\" loading=\"lazy\" /><figcaption>Fig 19. 엔티티 관계 정보 손실과 어텐션 도입을 통한 해결</figcaption></figure>\n<p>초기에는 MAPPO 기반의 팀 협동 플레이 보상을 설계하여 협동성을 유도하려 했습니다. 그러나 Strike / Support / Vanguard 3가지 역할 간의 협동을 하나의 스칼라 보상으로 정의하는 것이 모호하고, 보상 해킹(Reward Hacking)이 빈번히 발생했습니다.</p>\n<p>따라서 협동성을 보상으로 강제하는 접근을 포기하고, 모델 아키텍처 차원에서 에이전트가 팀원 간의 관계를 자연스럽게 학습하도록 방향을 전환했습니다. 그 결과, Cross-Attention을 도입하여 에이전트에게 각 엔티티의 중요도를 인식하게 하였지만 \"적 2명이 같은 거점에 집결\" 같은 엔티티 간 공간 패턴을 인식하지 못하고 이미 점령 중인 거점에 중복 배치되는 현상이 발생하였습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Cause</h4>\n<p>218-dim 관측 벡터에서 각 엔티티 슬롯은 선형 인코더(<code>nn.Linear</code>)를 통해 독립적으로 임베딩됩니다. 이후 Self Token이 Cross-Attention으로 엔티티 집합을 조회하지만, Query가 Self Token 1개이므로 <strong>엔티티 간 상대적 관계</strong>(밀집도, 협공 패턴, 동일 거점 중복)는 Attention weight에 반영되지 않았습니다. Cross-Attention의 출력은 \"각 엔티티가 Self에게 얼마나 중요한가\"의 가중합이지, \"엔티티들이 서로 어떤 관계인가\"의 정보는 아니었습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Goal</h4>\n<p>Cross-Attention이 출력하는 \"각 엔티티의 중요도\" 이전에 \"각 엔티티의 관계 정보\" 등을 추가로 입력하는 레이어의 추가.</p>\n<hr class=\"project-divider\" />\n<h4>Solution</h4>\n<p><strong>Intra-Set Self-Attention (Zambaldi et al., 2018)</strong></p>\n<p>각 엔티티 그룹(아군 / 적 / 거점)에 대해, Cross-Attention 이전에 <strong>Self-Attention 레이어</strong>를 삽입하여 엔티티들이 서로를 참조하도록 했습니다. Self-Attention을 거친 엔티티 토큰은 \"나와 같은 거점 근처에 있는 아군이 2명이다\"와 같은 문맥 정보를 내포하게 되며, 이후 Cross-Attention에서 Self Token이 이 <strong>문맥화된</strong> 엔티티 정보를 집약합니다.</p>\n<figure class=\"project-code-block\"><figcaption>policy.py — Relational Self-Attention pipeline</figcaption><pre><code class=\"language-python\"># 1. 선형 인코딩: 원본 특성 → hidden 차원\r\na_enc = self.ally_enc(allies)                  # (B, 8, 64)\r\n\r\n# 2. Self-Attention: 아군 토큰끼리 상호 참조\r\n#    → \"슬롯 3과 슬롯 5가 같은 거점 근처에 있다\" 등의 관계를 학습\r\na_rel, _ = self.ally_self_attn(a_enc, a_enc, a_enc,\r\n                               key_padding_mask=ally_mask)\r\n\r\n# 3. Residual + LayerNorm: 원본 정보 보존 + 학습 안정화\r\na_enc = self.ally_ln(a_enc + a_rel)            # (B, 8, 64)\r\n\r\n# 4. Cross-Attention: Self Token이 문맥화된 아군 정보를 집약\r\na_ctx, _ = self.ally_attn(q, a_enc, a_enc,\r\n                          key_padding_mask=ally_mask)  # (B, 1, 64)</code></pre></figure>\n<p><strong>패딩 마스크 처리</strong>: C++ 관측 레이아웃의 <code>0=유효, 1=패딩</code> 마스크를 Self-Attention과 Cross-Attention 양쪽에 동일하게 적용합니다. <code>_safe_mask()</code>가 모든 슬롯이 패딩인 경우 슬롯 0을 강제 언마스크하여 NaN을 방지합니다. C++ 측 수정 없이 Python 정책만으로 완결됩니다.</p>\n<figure class=\"project-code-block\"><figcaption>policy.py — safe mask</figcaption><pre><code class=\"language-python\">def _safe_mask(m: torch.Tensor) -&gt; torch.Tensor:\r\n    all_masked = m.all(dim=1, keepdim=True)   # (B, 1)\r\n    return m &amp; ~all_masked                     # 모든 슬롯 패딩 시 슬롯 0 언마스크</code></pre></figure>\n<hr class=\"project-divider\" />\n<h4>Result</h4>\n<p><strong>설계 제약과 Trade-off</strong></p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>항목</th><th>값</th></tr></thead><tbody><tr><td>파라미터 증가</td><td>168K → 268K (+60%)</td></tr><tr><td>추론 레이턴시</td><td>< 2ms (0.3초 스텝 예산 대비 0.7%)</td></tr><tr><td>ONNX 호환성</td><td>opset 14 — UE5 NNE 변경 없음</td></tr><tr><td>C++ 수정</td><td>없음 (패딩 마스크 레이아웃 재사용)</td></tr></tbody></table></div>\n<p>실증 결과는 <strong>\"결과: Attention 패턴 실증 분석\"</strong> 에서 확인 할 수 있습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Problem 3: 스텝 속도와 에이전트 이동 간의 타이밍 불일치</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/problem3.png\" alt=\"Fig 20. 스로틀링 제어\" loading=\"lazy\" /><figcaption>Fig 20. 스로틀링 제어</figcaption></figure>\n<p>에이전트가 이전 스텝에서 결정된 목적지에 도달하기 전에 새로운 스텝이 실행되어 이동이 취소되는 문제가 발생하여 학습의 품질이 크게 훼손되었습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Cause</h4>\n<p>학습 환경에서 <code>AGymConnectorManager</code>의 <code>Tick()</code>이 매 프레임(60Hz+) <code>Connector-&gt;Step()</code>을 호출하였습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Goal</h4>\n<p>스텝 주기를 에이전트의 EQS 기반 이동 완료 시간에 맞게 조율하여, 에이전트가 목적지까지 실제로 이동한 후 관측이 수집되도록 보장합니다.</p>\n<hr class=\"project-divider\" />\n<h4>Solution</h4>\n<p><strong><code>AGymConnectorManager</code> 오버라이딩</strong></p>\n<p>Schola의 학습 루프 진입점 구조는 다음과 같습니다.</p>\n<figure class=\"project-code-block\"><pre><code>AGymConnectorManager::Tick()\n    └─ UAbstractGymConnector::Step()          ← 1회 학습 스텝\n           ├─ ResolveEnvironmentStateUpdate()  ← Python에서 액션 수신 (gRPC, ~10ms 블로킹)\n           ├─ HandleStep() / HandleReset()     ← 환경에 액션 적용\n           └─ SubmitState()                    ← 관측·보상 Python으로 전송</code></pre></figure>\n<p><code>UAbstractGymConnector::Step()</code>은 Python과의 전체 한 사이클을 원자적으로 처리하며, 내부에서 <code>ResolveEnvironmentStateUpdate()</code>가 gRPC 응답을 블로킹 대기합니다. 따라서 <strong>스텝 호출 빈도를 제어하는 유일한 지점</strong>은 <code>Step()</code>을 직접 호출하는 <code>AGymConnectorManager::Tick()</code>입니다. 커넥터 내부 구현을 수정하지 않고 오직 <code>Tick()</code> 오버라이드만으로 스텝 속도를 외부에서 제어할 수 있다고 판단했습니다.</p>\n<hr class=\"project-divider\" />\n<p><strong><code>ADEGymConnectorManager</code> 구현</strong></p>\n<p><code>AGymConnectorManager</code>를 상속하는 커스텀 클래스 <code>ADEGymConnectorManager</code>를 구현하고, <code>AGymConnectorManager::Tick()</code> 대신 <code>AActor::Tick()</code>를 직접 호출하여 <code>Connector-&gt;Step()</code> 호출 빈도를 <code>StepInterval</code> 변수로 제어합니다.</p>\n<figure class=\"project-code-block\"><figcaption>ADEGymConnectorManager.h</figcaption><pre><code class=\"language-cpp\">// 에디터에서 스텝 간격 조정 가능\r\nUPROPERTY(EditAnywhere, Category = \"Schola|Throttling\",\r\n    meta = (ClampMin = \"0.01\", ClampMax = \"10.0\"))\r\nfloat StepInterval = 0.3f;  // 초 단위, 기본 2Hz</code></pre></figure>\n<p><code>StepInterval = 0.3s</code>는 EQS 48샘플 쿼리 소요 시간(~5ms) + NavMesh 경로 계산(~2ms) + 에이전트 이동 거리(최대 탐색 반경 600cm, 이동 속도 600cm/s 기준 ~1초)를 고려한 값입니다. 에이전트가 목적지까지 충분히 이동한 후 관측이 수집되는 최소 주기를 프로파일링하여 결정했습니다.</p>\n<figure class=\"project-code-block\"><figcaption>ADEGymConnectorManager.cpp — Implementation of Tick throttling</figcaption><pre><code class=\"language-cpp\">void ADEGymConnectorManager::Tick(float DeltaTime)\r\n{\r\n    // AGymConnectorManager::Tick 우회 — 직접 매 프레임 Step 호출 방지\r\n    AActor::Tick(DeltaTime);\r\n\r\n    if (!Connector) return;\r\n\r\n    // 연결 대기 단계: Step은 내부적으로 CheckForStart()만 실행, 블로킹 없음\r\n    if (Connector-&gt;IsNotStarted())\r\n    {\r\n        Connector-&gt;Step();\r\n        return;\r\n    }\r\n\r\n    // 실행 단계: StepInterval마다 1회 Step 호출\r\n    if (Connector-&gt;IsRunning())\r\n    {\r\n        // 에디터 백그라운드 전환 후 DeltaTime 급등으로 인한 버스트 스텝 방지\r\n        const float ClampedDelta = FMath::Min(DeltaTime, StepInterval);\r\n        StepAccumulator += ClampedDelta;\r\n        if (StepAccumulator &gt;= StepInterval)\r\n        {\r\n            StepAccumulator = 0.0f;\r\n            Connector-&gt;Step();\r\n        }\r\n    }\r\n}</code></pre></figure>\n<p>에디터 내 <code>BP_GymConnectorManager</code>의 부모 클래스를 <code>ADEGymConnectorManager</code>로 변경하는 것만으로 적용이 완료됩니다. <code>StepInterval</code>을 에디터 디테일 패널에서 값을 직접 조정할 수 있어 재빌드 없이 타이밍을 튜닝할 수 있습니다.</p>\n<hr class=\"project-divider\" />\n<h4>Result</h4>\n<p><code>StepInterval = 0.3s</code> 기준으로 에이전트가 EQS 목적지에 완전히 도달한 뒤 다음 관측이 수집되어 학습 데이터 품질이 개선되었습니다. 에디터 백그라운드 전환 시 발생하던 <code>DeltaTime</code> 급등에 의한 버스트 스텝 역시 <code>FMath::Min(DeltaTime, StepInterval)</code> 클램핑으로 차단되었습니다. <code>StepInterval</code> 단일 변수로 학습 속도와 이동 완료율 사이의 트레이드오프를 재빌드 없이 조절할 수 있는 구조가 완성되었습니다.</p>\n<hr class=\"project-divider\" />\n<h3>결과 (Results)</h3>\n<h4>학습 결과</h4>\n<p>총 2.5M(250만) 타임스텝에 걸쳐 MAPPO 기반 ScriptedAI 대전 학습을 수행했습니다.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/result_reward.png\" alt=\"Fig 21. reward\" loading=\"lazy\" /><figcaption>Fig 21. reward</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/result_vf.png\" alt=\"Fig 22. vf explained\" loading=\"lazy\" /><figcaption>Fig 22. vf explained</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/result_entropy.png\" alt=\"Fig 23. entropy\" loading=\"lazy\" /><figcaption>Fig 23. entropy</figcaption></figure>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>핵심 수치 요약</h4>\n<div class=\"project-table-wrap\"><table><thead><tr><th>지표</th><th>값</th><th>의미</th></tr></thead><tbody><tr><td><strong>episode_mean</strong></td><td>0 → 40, 17, 48 수렴</td></tr><tr><td><strong>vf/explained_var</strong></td><td>0.8 이상</td><td>Critic이 미래 보상의 80% 이상을 설명 — 높은 상태 가치 예측력</td></tr><tr><td><strong>entropy</strong></td><td>초기 상승 후 하락 중</td><td>탐험→활용 전환 확인</td></tr></tbody></table></div>\n<p><strong>학습 종료 판단:</strong> lr 스케줄러가 0에 도달하고 reward가 plateau에 진입한 2.5M 스텝에서 학습을 종료했습니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<hr class=\"project-divider\" />\n<h4>승률</h4>\n<p>스크립티드 AI와의 승률은 초기 0%에서 70% 수준으로 상승했습니다. 중간의 승률 저하는 스크립티드 AI의 티어가 상승하면서 발생한 일시적인 현상으로, 이후 다시 승률을 회복하면서 RL 에이전트가 정상적으로 게임 규칙을 학습했음을 확인했습니다.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/win_rate.png\" alt=\"Fig 24. win rate\" loading=\"lazy\" /><figcaption>Fig 24. win rate</figcaption></figure>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<hr class=\"project-divider\" />\n<h4>기타 시행 착오</h4>\n<p><strong>1. RL 행동 공간 정의 — 세부 제어에서 전략적 포지셔닝으로</strong></p>\n<p>초기 설계에서는 이동 방향, 속도, 조준 등 저수준(Low-level) 행동을 RL로 직접 제어하려 했습니다. 그러나 로컬 환경에서의 실험 결과, 수렴에 필요한 샘플 수가 현실적인 학습 시간 내에 달성 불가능한 수준임을 확인했습니다.</p>\n<p>이에 Unreal Engine의 기존 내비게이션·EQS 시스템을 활용해 저수준 행동을 위임하고, RL은 <strong>전략적 포지셔닝</strong>만 담당하는 방향으로 전환했습니다. 구체적으로는 EQS 쿼리에 사용되는 7차원 가중치 벡터를 RL 정책의 출력으로 정의하여, 정책이 \"어디에 위치할 것인가\"를 결정하고 실제 이동은 UE5 시스템에 위임하는 구조로 수렴했습니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>2. 셀프 플레이 — 복잡한 환경에서의 정책 붕괴</strong></p>\n<p>MuZero에서 영감을 받아 초기에는 셀프 플레이(Self-Play) 방식으로 처음부터 학습을 시도했습니다. 그러나 3가지 역할 타입(Strike / Support / Vanguard), 변동하는 아군·적 수, 거점 점령 상태 등 환경 복잡도가 높은 상황에서는 양측이 동시에 발전하는 과정에서 보상이 수렴하지 않고 정책 붕괴(Policy Collapse)가 반복적으로 발생했습니다.</p>\n<p>이를 해결하기 위해 초반 게임 규칙의 학습을 담당할 고정된 스크립트 AI 대전으로 학습을 수행하고, 이후에 셀프 플레이와 스크립트 AI를 믹싱하여 성능을 극대화했습니다.</p>\n<hr class=\"project-divider\" />\n<h4>향후 계획</h4>\n<p><strong>RL 에이전트의 난이도 구현</strong></p>\n<ul><li>RL 에이전트의 난이도를 구현하기 위해 다양한 방법의 실험 예정(학습시간으로 조정할지, Temperature를 부여할지)</li><li>난이도별 모델을 별도로 학습해야하는지, 하나의 모델에 난이도별 파라미터를 부여할지 실험하여 비교할 예정입니다.</li></ul>\n<p><strong>AWS 클라우드에서의 다중 EC2 인스턴스 활용</strong></p>\n<ul><li>현재는 하나의 EC2 인스턴스에서 4개의 UE5를 실행하여 학습하는 상황입니다. Ray 클러스터를 활용하여 다중 EC2 인스턴스 분산 학습 환경을 구축할 예정입니다.</li></ul>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<hr class=\"project-divider\" />\n<h4>Attention 패턴 실증 분석</h4>\n<p>학습된 체크포인트에서 Attention weight를 직접 추출하여 파이프라인 전체(Self-Attention → Cross-Attention)가 설계 의도대로 동작하는지 검증했습니다.</p>\n<hr class=\"project-divider\" />\n<h4>실험 설계</h4>\n<p>동일한 학습 정책에 두 가지 대조 시나리오를 입력하여 Attention 분포 변화를 관찰했습니다.</p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>시나리오</th><th>설명</th></tr></thead><tbody><tr><td><strong>Clustered</strong></td><td>아군 4명이 동일 거점 반경 내에 밀집 (위치 차이 ≈ 0.01)</td></tr><tr><td><strong>Spread</strong></td><td>아군 4명이 맵 4개 코너에 분산 배치 (위치 차이 ≈ 0.6)</td></tr></tbody></table></div>\n<p>슬롯 4–7의 Attention weight는 전 역할·전 시나리오에서 0.00으로, 패딩 마스크가 정상 적용되어 유효 엔티티(슬롯 0–3)에만 집중됨을 확인했습니다.</p>\n<hr class=\"project-divider\" />\n<h4>1단계 — Intra-Set Self-Attention: 엔티티 간 공간 관계 포착</h4>\n<p>Self-Attention은 Cross-Attention의 전처리 단계로, 엔티티 토큰들이 서로를 참조해 맥락화된 표현을 생성합니다. 핵심 지표는 <strong>대형 변화(Clustered → Spread)에 따른 가중치 분포의 변동폭(Δmax)</strong> 입니다.</p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>역할</th><th>대형 민감도</th><th>주요 관찰</th></tr></thead><tbody><tr><td><strong>Strike</strong></td><td>중간 (Δmax ≈ 0.19)</td><td>Clustered: 4슬롯 균등 분배 → Spread: 슬롯 1(Assault) 집중(0.47)</td></tr><tr><td><strong>Support</strong></td><td>낮음 (Δmax ≈ 0.06)</td><td>대형 무관, 전 슬롯 균등 유지 — 역할 특성과 일치</td></tr><tr><td><strong>Vanguard</strong></td><td>높음 (Δmax ≈ 0.29)</td><td>Clustered: 슬롯 3 집중(0.53) → Spread: 슬롯 1 집중(0.54)</td></tr></tbody></table></div>\n<figure class=\"project-media\"><img src=\"/images/project1/attn_comparison_strike.png\" alt=\"Fig 25. STRIKE Self-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 25. STRIKE Self-Attention, Clustered vs Spread vs Difference</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/attn_comparison_support.png\" alt=\"Fig 26. SUPPORT Self-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 26. SUPPORT Self-Attention, Clustered vs Spread vs Difference</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/attn_comparison_vanguard.png\" alt=\"Fig 27. VANGUARD Self-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 27. VANGUARD Self-Attention, Clustered vs Spread vs Difference</figcaption></figure>\n<p>Support가 가장 낮은 민감도를 보이는 것은 버그가 아닌 <strong>역할 특성의 자연스러운 내재화</strong>입니다. 회복 역할은 팀 대형에 관계없이 전체 아군을 동등하게 모니터링해야 하며, 학습이 이를 반영했습니다. 반면 Vanguard는 가장 높은 민감도를 보여, 전선 앵커 역할이 팀 배치 변화에 가장 민감하게 반응하도록 분화됐음을 나타냅니다.</p>\n<hr class=\"project-divider\" />\n<h4>2단계 — Cross-Attention: 행동 직전 최종 정보 집약</h4>\n<p>Cross-Attention은 Self Token(자신의 관측 임베딩)이 Query가 되어 아군·적·거점 세 엔티티 집합 각각을 조회하는 단계입니다. 이 가중합이 직접 EQS 가중치(행동)로 이어지므로, \"에이전트가 실제 행동 결정 시 무엇을 보는가\"를 가장 직접적으로 드러냅니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>적(Enemy) Cross-Attention</strong></p>\n<p>전 역할에서 활성 적 슬롯 0·1에 가중치가 거의 균등하게 분배됩니다(≈ 0.48–0.52). 두 적을 동등한 위협으로 인식하는 일관된 패턴으로, 슬롯 2–7(패딩)은 정확히 0으로 억제됩니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>거점(Base) Cross-Attention</strong></p>\n<p>역할마다 우선 거점이 명확히 다릅니다. Strike와 Support는 거점 0·1에 집중(≈ 0.40–0.42)하는 반면, Vanguard는 거점 1·2에 더 분산된 가중치를 보입니다. 전선 유지 역할인 Vanguard가 중립 거점 및 후방 거점을 균형 있게 참조하도록 분화된 결과로 해석됩니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>아군(Ally) Cross-Attention — Self-Attention과의 일관성 검증</strong></p>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_strike_clustered.png\" alt=\"Fig 28. STRIKE Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 28. STRIKE Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_strike_spread.png\" alt=\"Fig 29. SUPPORT Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 29. SUPPORT Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_vanguard_clustered.png\" alt=\"Fig 30. VANGUARD Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 30. VANGUARD Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_vanguard_spread.png\" alt=\"Fig 31. SUPPORT Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 31. SUPPORT Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_support_clustered.png\" alt=\"Fig 32. SUPPORT Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 32. SUPPORT Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_support_spread.png\" alt=\"Fig 33. SUPPORT Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 33. SUPPORT Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure></div>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p>Vanguard의 Cross-Attention 아군 가중치는 Self-Attention 결과와 정확히 대응합니다. Clustered에서 Self-Attention이 슬롯 3을 지배적으로 선택했고, Cross-Attention도 슬롯 3을 가장 높게 참조합니다(0.50). Spread에서는 양쪽 모두 슬롯 1로 초점이 이동합니다(Self: 0.54, Cross: 0.42). 이는 Self-Attention이 생성한 맥락화된 표현을 Cross-Attention이 일관성 있게 활용함을 보여주며, <strong>두 단계 파이프라인이 의도대로 연결되어 작동함이 확인되었습니다.</strong></p>\n<hr class=\"project-divider\" />\n<h4>종합</h4>\n<div class=\"project-table-wrap\"><table><thead><tr><th>검증 항목</th><th>결과</th></tr></thead><tbody><tr><td>패딩 마스크 억제</td><td>슬롯 4–7 완전 억제 — 정상</td></tr><tr><td>역할별 Self-Attention 분화</td><td>Support(균등) / Strike(중간) / Vanguard(고민감) — 역할 특성과 일치</td></tr><tr><td>Cross-Attention 엔티티 우선순위</td><td>적: 균등 위협 인식 / 거점: 역할별 상이 / 아군: Self-Attention과 일관</td></tr><tr><td>파이프라인 일관성</td><td>Self → Cross Attention 간 초점 대상 일치 — 파이프라인 정합성 확인</td></tr></tbody></table></div>\n<p>개별 역할 레이블 외 별도의 귀납 편향 없이, Intra-Set Self-Attention 구조만으로 <strong>역할 특화된 공간 추론의 자발적 분화</strong>가 달성됐음을 두 단계의 Attention 패턴을 통해 실증했습니다.</p>",
        "en": "<hr class=\"project-divider\" />\n<h3>Overview</h3>\n<p>This project involved designing and implementing a Dynamic EQS plugin that automatically optimizes the EQS (Environment Query System) weights of UE5 agents through reinforcement learning, validated in a 5v5 team-based capture point environment using MAPPO. The result is a hybrid AI system in which the EQS handles agent spatial navigation and movement, while the reinforcement learning model is responsible for determining optimal positioning.</p>\n<p>To ensure training stability, an attention mechanism was introduced at the architecture level rather than relying on cooperative reward scalar design to encourage team-based collaborative behavior. This allows each agent to perceive its teammates and selectively identify critical state information, enabling cooperative strategies to emerge naturally.</p>\n<p>The entire system was built around a RL-EQS integration middleware layer on top of Schola (gRPC), abstracted as a plugin for reusability across other UE5 projects. The full pipeline — from system design through to AWS-based parallel training infrastructure — was implemented end-to-end by a single developer.</figure></div>\n<hr class=\"project-divider\" />\n<h3>System Architecture</h3>\n<figure class=\"project-media\"><img src=\"/images/project1/archi.png\" alt=\"System Architecture\" loading=\"lazy\" /><figcaption>Fig 1. System Architecture and Hierarchical Positioning Workflow</figcaption></figure>\n<h3>Tech Stack</h3>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Category</th><th>Technologies</th></tr></thead><tbody><tr><td><strong>Game Engine</strong></td><td>Unreal Engine 5.6 (C++17)</td></tr><tr><td><strong>RL Framework</strong></td><td>Ray RLlib 2.7, PyTorch</td></tr><tr><td><strong>UE5-Python Bridge</strong></td><td>Schola Plugin (gRPC-based)</td></tr><tr><td><strong>Neural Network Inference</strong></td><td>ONNX Runtime via UE5 NNE (Neural Network Engine)</td></tr><tr><td><strong>Ability System</strong></td><td>UE5 Gameplay Ability System (GAS) — GameplayAbility, GameplayEffect, GameplayTag</td></tr><tr><td><strong>Cloud & Infra</strong></td><td>AWS (EC2, EKS), Docker (Linux)</td></tr><tr><td><strong>Communication</strong></td><td>gRPC (Schola protocol)</td></tr><tr><td><strong>Monitoring</strong></td><td>TensorBoard</td></tr></tbody></table></div>\n<hr class=\"project-divider\" />\n<h3>Training Environment</h3>\n<p>The environment is a <strong>5v5 team-based Capture the Point</strong> mode featuring 5 capture points across the map. The team that captures and holds more points to reach the target score first wins the match.</p>\n<p>-----</p>\n<h4>Class Role Design</h4>\n<p>Each agent is assigned a specific class—<strong>Strike, Vanguard, or Support</strong>—at the start of every episode. Since each class requires a fundamentally different positioning strategy, I assigned <strong>independent policy networks</strong> to each class and applied role-specific reward functions.</p>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/gifs/project1/project1_strike.gif\" alt=\"\" loading=\"lazy\" /></figure><figure class=\"project-media\"><img src=\"/gifs/project1/project1_vanguard.gif\" alt=\"\" loading=\"lazy\" /></figure><figure class=\"project-media\"><img src=\"/gifs/project1/project1_support.gif\" alt=\"\" loading=\"lazy\" /></figure></div>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Class</th><th>Role</th><th>Core Positioning Objective</th></tr></thead><tbody><tr><td><strong>Strike</strong></td><td>Ranged DPS</td><td>Approach enemy points + Maintain optimal firing range</td></tr><tr><td><strong>Vanguard</strong></td><td>Melee Tank</td><td>Maintain front-line presence + Close-quarters engagement</td></tr><tr><td><strong>Support</strong></td><td>Backline Healer</td><td>Track injured allies + Maintain safe rear positioning</td></tr></tbody></table></div>\n<p>-----</p>\n<h4>Training Against Scripted AI</h4>\n<p>The Blue Team (RL) trains against the <strong>Red Team (Scripted AI)</strong>, which operates using hardcoded EQS weights and State Machines.</p>\n<p>Due to the complex mix of the three agent classes, which makes Self-Play less effective in the early stages, Scripted AI was chosen to provide a consistent difficulty baseline. This allowed the RL agents to learn foundational strategies reliably.</p>\n<p>-----</p>\n<h4>Curriculum Tier System</h4>\n<p>The Scripted AI is divided into <strong>three difficulty tiers</strong>: 1 (Basic), 2 (Standard), and 3 (Aggressive). When the RL team's win rate exceeds a specific promotion threshold over a set period, the system automatically transitions to the next tier.</p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Tier Transition</th><th>Promotion Threshold</th></tr></thead><tbody><tr><td>Tier 1 → 2</td><td>25% or higher</td></tr><tr><td>Tier 2 → 3</td><td>40% or higher</td></tr></tbody></table></div>\n<p>This system was implemented to prevent overfitting to lower difficulties and to gradually expose the agents to stronger opponents. Tier information is stored in <code>scripted_ai_config.json</code>, and UE5’s <code>LoadTierFromConfig()</code> applies the update automatically during the next episode reset.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<hr class=\"project-divider\" />\n<h4>Observation Space</h4>\n<p>A <strong>218-dimensional Entity-Centric vector</strong> generated by <code>FDEObservationV2::ToFlatArray()</code>. It encodes allies, enemies, and strategic bases into fixed-size slot tokens. A separate padding mask is provided to ensure the Python-based <strong>MultiheadAttention</strong> mechanism processes only valid entities.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>Agent Input State Configuration</strong></p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Index</th><th>Dim</th><th>Token Content</th><th>Normalization & Details</th></tr></thead><tbody><tr><td><strong>[0 : 7]</strong></td><td>7</td><td>Self Token</td><td>Pos/7500(3) + Vel/600(3) + Health(1)</td></tr><tr><td><strong>[7 : 71]</strong></td><td>64</td><td>Ally Tokens (8×8)</td><td>Rel\\_Pos/8000(3) + HP(1) + Alive(1) + Class One-hot(3)</td></tr><tr><td><strong>[71 : 135]</strong></td><td>64</td><td>Enemy Tokens (8×8)</td><td>Rel\\_Pos/8000(3) + HP(1) + Visibility(1) + Class One-hot(3)</td></tr><tr><td><strong>[135 : 191]</strong></td><td>56</td><td>Base Tokens (8×7)</td><td>Rel\\_Pos/15000(2) + Height/1000(1) + Occupancy(1) + Progress(1) + Assigned(1) + Strat\\_Value(1)</td></tr><tr><td><strong>[191 : 199]</strong></td><td>8</td><td>Ally Mask</td><td>0 = Valid, 1 = Padding</td></tr><tr><td><strong>[199 : 207]</strong></td><td>8</td><td>Enemy Mask</td><td>0 = Valid, 1 = Padding</td></tr><tr><td><strong>[207 : 215]</strong></td><td>8</td><td>Base Mask</td><td>0 = Valid, 1 = Padding</td></tr><tr><td><strong>[215 : 218]</strong></td><td>3</td><td>Class One-hot</td><td>[strike, vanguard, support]</td></tr><tr><td><strong>TOTAL</strong></td><td><strong>218</strong></td><td></td><td></td></tr></tbody></table></div>\n<blockquote><strong>Mask Handling:</strong> The <code>_safe_mask()</code> function in the Python policy prevents <strong>NaN</strong> errors in MultiheadAttention by forcibly unmasking Slot 0 when all slots are padded. The mask threshold is set to <code>&gt; 0.5</code> (float comparison), preserving the <code>0.0=Valid / 1.0=Padding</code> semantics.</blockquote>\n<hr class=\"project-divider\" />\n<h4>Action Space</h4>\n<p>The policy network output is a <strong>7-dimensional Continuous Box Action</strong>. Each dimension corresponds to a weight parameter for an individual test within the <strong>EQS (Environment Query System)</strong> query. These weights dynamically adjust the contribution of each test when the EQS evaluates 48 candidate positions.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Index</th><th>Parameter Name</th><th>Range</th><th>Description</th></tr></thead><tbody><tr><td><strong>[0]</strong></td><td><code>EnemyObjectiveProximity</code></td><td>[-1, 1]</td><td>Drive to move toward enemy objectives (Positive: approach, Negative: evade).</td></tr><tr><td><strong>[1]</strong></td><td><code>AllyObjectiveProximity</code></td><td>[-1, 1]</td><td>Drive to defend allied objectives (Positive: return, Negative: advance).</td></tr><tr><td><strong>[2]</strong></td><td><code>CoverDensity</code></td><td>[-1, 1]</td><td>Preference for areas with high cover (Positive: prioritize cover, Negative: prefer open terrain).</td></tr><tr><td><strong>[3]</strong></td><td><code>EnemyVisibility</code></td><td>[-1, 1]</td><td>Drive to maintain line-of-sight with enemies (Positive: maintain vision, Negative: conceal).</td></tr><tr><td><strong>[4]</strong></td><td><code>AllyProximity</code></td><td>[-1, 1]</td><td>Drive to maintain proximity to allies (Positive: cluster, Negative: disperse).</td></tr><tr><td><strong>[5]</strong></td><td><strong><code>CombatRange</code></strong></td><td>[-1, 1]</td><td>Adjustment of preferred engagement distance (Positive: maintain distance, Negative: close in).</td></tr><tr><td><strong>[6]</strong></td><td><code>AssignedBaseProximity</code></td><td>[-1, 1]</td><td>Attraction toward the assigned objective (Positive: approach, Negative: depart).</td></tr><tr><td><strong>TOTAL</strong></td><td><strong>7-dim</strong></td><td><strong>[-1, 1]</strong></td><td><code>FDEEQSWeightParameters</code></td></tr></tbody></table></div>\n<p>The output layer of the policy network utilizes a <code>tanh</code> activation function to naturally constrain each weight within the <strong>[-1, 1]</strong> range. These seven weights are injected into the EQS query parameters via <code>UDynamicEQSExecutor::ApplyWeightsToRequest()</code>. Based on these inputs, the EQS calculates a comprehensive score for <strong>48 candidate positions</strong> to determine the optimal movement target.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<hr class=\"project-divider\" />\n<h4>Reward Structure</h4>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>S T R I K E</h4>\n<p>The objective is to capture bases while maintaining high damage output from a distance. An <strong>approach reward</strong> is granted per step proportional to the distance closed toward the target base, with an additional <strong>presence bonus</strong> upon entering the capture radius. To ensure role adherence, a penalty is applied if the agent gets too close to enemies.</p>\n<figure class=\"project-code-block\"><figcaption>Strike reward (per step)</figcaption><pre><code class=\"language-cpp\">reward = 0\r\nif distance_to_target_base decreased:\r\n    reward += approach_reward * delta_distance\r\nif agent inside target_base radius:\r\n    reward += zone_presence_bonus\r\nif base just captured:\r\n    activate momentum_bonus for PostCaptureMomentumDuration steps\r\n    reward += capture_bonus\r\nif momentum active:\r\n    reward += momentum_bonus  # encourages moving to next base\r\nif distance_to_enemy &lt; MinCombatRange:\r\n    reward -= too_close_enemy_penalty  # ranged: maintain combat distance</code></pre></figure>\n<hr class=\"project-divider\" />\n<h4>V A N G U A R D</h4>\n<p>The objective is to capture bases while engaging in frontline melee combat. The base approach and capture logic follow the same principles as the Strike class. A <strong>melee engagement bonus</strong> is awarded when enemies are within close range inside a base, reinforcing frontline tanking behavior.</p>\n<figure class=\"project-code-block\"><figcaption>Vanguard reward (per step)</figcaption><pre><code class=\"language-cpp\">reward = 0\r\nif distance_to_target_base decreased:\r\n    reward += approach_reward * delta_distance\r\nif agent inside target_base radius:\r\n    reward += zone_presence_bonus\r\n    if enemy within melee range:\r\n        reward += melee_range_bonus  # frontline tank: reward close engagement\r\nif base just captured:\r\n    activate momentum_bonus for PostCaptureMomentumDuration steps\r\n    reward += capture_bonus\r\nif momentum active:\r\n    reward += momentum_bonus</code></pre></figure>\n<hr class=\"project-divider\" />\n<h4>S U P P O R T</h4>\n<p>The objective is to track, heal, and maintain rear-guard positioning for wounded allies. To prevent \"jittery\" behavior (oscillating between targets), a <strong>5-step target cache</strong> is implemented. Agents receive a <strong>rear-positioning bonus</strong> for staying behind non-support allies.</p>\n<figure class=\"project-code-block\"><figcaption>Support reward (per step)</figcaption><pre><code class=\"language-cpp\">reward = 0\r\nif staleness_counter &gt;= 5 or no cached_target:\r\n    cached_target = ally with lowest health\r\n    staleness_counter = 0\r\nelse:\r\n    staleness_counter++\r\n\r\nif distance_to_cached_target decreased:\r\n    reward += approach_reward * delta_distance\r\nif heal applied to ally:\r\n    reward += heal_reward * heal_amount\r\nif agent positioned behind cached_target (rear arc):\r\n    reward += rear_positioning_bonus\r\nif cached_target.health &lt; threshold and agent attempted kill:\r\n    reward -= role_deviation_penalty</code></pre></figure>\n<hr class=\"project-divider\" />\n<h4>Team Reward Mixing (MAPPO Cooperative Signal)</h4>\n<p>To enhance cooperation in team-based environments, we implement a <strong>Reward Mixing</strong> strategy that blends individual and team-average rewards.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p>$<span class=\"project-inline-math\">\\text{final\\_reward} = (1 - \\alpha) \\times r_{\\text{individual}} + \\alpha \\times r_{\\text{team\\_avg}}</span>$</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Parameter</th><th>Value</th><th>Design Intent</th></tr></thead><tbody><tr><td><code>TeamRewardMixingRatio</code> (α)</td><td><strong>0.2</strong></td><td>Maintains individual role optimization as the primary signal while preventing selfish behavior through a 20% cooperative signal.</td></tr></tbody></table></div>\n<p>The ratio is set to <strong><span class=\"project-inline-math\">\\alpha = 0.2</span></strong> to allow agents to first converge on basic role-specific behaviors (positioning, range maintenance) before refining team-level coordination. The team average is calculated based on the <code>LastIndividualStepReward</code> of teammates (excluding the self). Mixing is automatically disabled for solo agents. This calculation occurs every step in <code>DERewardSubsystem.cpp</code> via the <code>CalculateStepReward()</code> function.</p>\n<hr class=\"project-divider\" />\n<h4>UE5 → Python Reward Pipeline</h4>\n<p>Rewards are calculated in <strong>C++ (UE5)</strong>, normalized in <strong>Python (RLlib)</strong>, and then utilized for PPO updates.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/rewardpipeline.png\" alt=\"Fig 2. Reward Pipeline\" loading=\"lazy\" /><figcaption>Fig 2. Reward Pipeline</figcaption></figure>\n<p><strong>Workflow:</strong></p>\n<ol><li><strong>C++ Calculation:</strong> <code>DERewardSubsystem</code> computes per-step rewards (approach, objective, combat), applies 80:20 team mixing, and performs initial normalization via <code>RewardScale</code> and <code>Clamp</code>.</li><li><strong>gRPC Transmission:</strong> The <strong>Schola plugin</strong> transmits reward data to the Python environment (<code>DEEntityCentricEnv</code>) via gRPC.</li><li><strong>Python Normalization:</strong> <code>process_reward()</code> applies a <code>reward_scale=0.01</code> and clips the value to <code>±5.0</code> to ensure PPO training stability.</li><li><strong>Dual Critic Evaluation:</strong> The <code>EntityCentricRLlibModel</code> value function utilizes a learnable mixing coefficient (α, initial sigmoid 0.5) to combine <strong><span class=\"project-inline-math\">V_{local}</span></strong> (226-dim Attention Critic) and <strong><span class=\"project-inline-math\">V_{central}</span></strong> (71-dim Global MLP Critic). All three role policies share the centralized critic.</li></ol>\n<hr class=\"project-divider\" />\n<h3>Key Features</h3>\n<h4>1. Dynamic-EQS Plugin</h4>\n<p><strong>DynamicEQS</strong> is a <strong>reusable UE5-exclusive RL-EQS integration layer</strong> built upon the Schola plugin. Game projects using DynamicEQS can inherit EQS-specific abstract classes instead of directly handling Schola's low-level gRPC/ONNX processing.</p>\n<p>DynamicEQS consists of four primary classes responsible for <strong>Environment</strong>, <strong>Agent</strong>, <strong>Action</strong>, and <strong>Observation</strong>. It serves as a middleware layer between Schola and the game project, configuring EQS weights and mapping them to the policy network.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/pluginarchi.png\" alt=\"Fig 3. Plugin Hierarchy\" loading=\"lazy\" /><figcaption>Fig 3. Plugin Hierarchy</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/flow3.png\" alt=\"Fig 4. Training/Inference Runtime Data Flow\" loading=\"lazy\" /><figcaption>Fig 4. Training/Inference Runtime Data Flow</figcaption></figure>\n<hr class=\"project-divider\" />\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>EQS Weight Injection</h4>\n<p>The following flow describes how the policy network's output (7-dim Box action) is injected as actual EQS query parameters.</p>\n<p><strong>Editor Setup</strong></p>\n<ol><li>Add the <code>UDynamicEQSExecutor</code> component to the <code>BP_Agent</code> (ADECharacter).</li><li>Enter the parameter names used in each EQS Query test into the <code>WeightParamNames</code> array in order (e.g., <code>EnemyObjectiveProximity</code>, <code>AllyObjectiveProximity</code>, etc.).</li><li>Set the <code>Actuator</code> to <code>UDETacticalParameterActuator</code> within the <code>UDEScholaAgent</code> component.</li></ol>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/eqs1.png\" alt=\"Fig 5. DynamicEQSExecutor Component\" loading=\"lazy\" /><figcaption>Fig 5. DynamicEQSExecutor Component</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/eqs2.png\" alt=\"Fig 6. EQS Asset Configuration\" loading=\"lazy\" /><figcaption>Fig 6. EQS Asset Configuration</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/eqsdebug1.png\" alt=\"Fig 7. EQS Debugging 1\" loading=\"lazy\" /><figcaption>Fig 7. EQS Debugging 1</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/eqsdebug2.png\" alt=\"Fig 8. EQS Debugging 2\" loading=\"lazy\" /><figcaption>Fig 8. EQS Debugging 2</figcaption></figure></div>\n<figure class=\"project-code-block\"><figcaption>EQS Parameter Injection</figcaption><pre><code class=\"language-cpp\">// DynamicEQSExecutor.cpp — EQS Parameter Injection\r\nvoid UDynamicEQSExecutor::ApplyWeightsToRequest(FEnvQueryRequest&amp; Request) const\r\n{\r\n    for (int32 i = 0; i &lt; Weights.Num(); ++i)\r\n    {\r\n        const FName ParamName = (WeightParamNames.IsValidIndex(i) &amp;&amp; !WeightParamNames[i].IsNone())\r\n            ? WeightParamNames[i]\r\n            : FName(*FString::Printf(TEXT(\"Weight%d\"), i));\r\n        Request.SetFloatParam(ParamName, Weights[i]);\r\n    }\r\n}</code></pre></figure>\n<hr class=\"project-divider\" />\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>Evaluation Mode</h4>\n<p>The <strong>Live Evaluation</strong> pipeline (<code>eval_live.py</code>) quantitatively validates the performance of checkpoints saved during training through matches against Scripted AI.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p>The <code>eval_live.py</code> pipeline is designed to quantitatively verify checkpoint performance against Scripted AI during the training process.</p>\n<p>By concurrently connecting two sub-environments in UE5, it evaluates two checkpoints (<code>best</code>, <code>latest</code>) in parallel. Episode outcomes are categorized into <code>win / loss / draw / timeout</code>, and the win rate based on 50 episodes is saved in <code>eval_results_&lt;timestamp&gt;.json</code>.</p>\n<p>The system is designed to run immediately via CPU inference by extracting only the Actor weights from <code>policy_state.pkl</code>, eliminating the need for a Ray runtime.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/eval.png\" alt=\"Fig 9. model eval result\" loading=\"lazy\" /><figcaption>Fig 9. model eval result</figcaption></figure>\n<hr class=\"project-divider\" />\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>Decoupling Game Logic via FInstancedStruct</h4>\n<p>To prevent the plugin from having direct members of game-specific types (such as <code>AssignedBaseIndex</code>), external parameters are stored opaquely using <code>FInstancedStruct</code>.</p>\n<p>If a plugin has a <strong>compile-time dependency</strong> on the game module, it becomes impossible to reuse in other projects. Therefore, a method was needed to break dependencies between modules while preserving runtime type information. <code>void*</code> lacks type safety, and the interface pattern (<code>IExternalContext</code>) forces implementation on the game side, compromising the plugin's independence. UE5's <code>FInstancedStruct</code> met these requirements by allowing opaque storage while preserving USTRUCT metadata.</p>\n<figure class=\"project-code-block\"><figcaption>DynamicEQSAgentComponent.h</figcaption><pre><code class=\"language-cpp\">UPROPERTY(BlueprintReadWrite)\r\nFInstancedStruct ExternalParams; // Can store any USTRUCT\r\n\r\n// DETacticalParameterActuator.cpp — Casting to game type at the point of use\r\nconst FDEAgentExternalContext* Ctx =\r\n    AgentComponent-&gt;ExternalParams.GetPtr\\&lt;FDEAgentExternalContext\\&gt;(); \r\nif (Ctx)\r\n    Weights.AssignedBaseProximity = ComputeBaseProximityWeight(Ctx-&gt;AssignedBaseIndex);</code></pre></figure>\n<p>Thanks to this pattern, the plugin does not include any game headers and can be reused as-is in other projects.</p>\n<hr class=\"project-divider\" />\n<h4>2. MAPPO (Multi-Agent PPO)</h4>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p>This project adopts <strong>MAPPO (Multi-Agent PPO)</strong> to train three role-specific independent actors (<code>strike_policy</code>, <code>vanguard_policy</code>, <code>support_policy</code>) alongside a shared centralized critic.</p>\n<p><strong>Centralized Critic</strong> — The <code>CentralizedCritic</code> estimates the overall team value by taking a 71-dim global team state (<code>FDETeamWorldState</code>: positions/HP/strategy of 5 allies + positions/reliability of 5 enemies + map state) as input. By directly referencing team-level information that cannot be captured by individual agent observations alone (e.g., ally distribution, overall capture progress), the critic significantly reduces the variance of Advantage estimation.</p>\n<hr class=\"project-divider\" />\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>Dual Value Estimation</strong> Each <code>EntityCentricRLlibModel</code> combines a local critic (<span class=\"project-inline-math\">V_{local}</span>, 226-dim agent observation) and a centralized critic (<span class=\"project-inline-math\">V_{central}</span>, 71-dim global state) using a learnable mixing coefficient <span class=\"project-inline-math\">\\alpha</span>.</p>\n<p>$<span class=\"project-inline-math\">V = \\alpha \\cdot V_{\\text{local}}(\\text{agent\\_obs}[0:226]) + (1 - \\alpha) \\cdot V_{\\text{central}}(\\text{global\\_state}[226:297])</span>$</p>\n<p><span class=\"project-inline-math\">\\alpha</span> is initialized via <code>sigmoid(_value_mix_logit)</code> (initial value of 0.5) and automatically determines the optimal weight between local and global signals for each role through training.</p>\n<div class=\"project-spacer\" style=\"--space:50px\"></div>\n<h4>Role of Self-Attention</h4>\n<p>Intra-Set Self-Attention is applied to each entity group (allies, enemies, bases) within the Actor's encoder (Zambaldi et al., 2018). This allows entity tokens to reference one another and learn spatial relationships within the set, such as <strong>\"Slot 3 and Slot 5 are clustering near the same base.\"</strong> This contextualized representation then serves as the input for Cross-Attention.</p>\n<div class=\"project-spacer\" style=\"--space:50px\"></div>\n<h4>Role of Cross-Attention</h4>\n<p>The Self Token (embedding of the 7-dim self-observation) acts as the <strong>Query</strong>, while the ally, enemy, and base tokens processed via Self-Attention serve as <strong>Keys/Values</strong>. Cross-Attention aggregates the importance of each entity relative to the agent's current state into a weighted sum, which is then used to determine the action (7-dim EQS weights).</p>\n<figure class=\"project-code-block\"><figcaption>train.py</figcaption><pre><code class=\"language-python\"># Independent policy routing by role\r\n\r\nSTRATEGY_POLICY_NAMES = {0: \"strike_policy\", 1: \"vanguard_policy\", 2: \"support_policy\"}\r\n\r\nconfig = config.multi_agent(\r\npolicies={\r\nname: PolicySpec(config={\"model\": model_cfg})\r\nfor name in STRATEGY_POLICY_NAMES.values()\r\n},\r\npolicy_mapping_fn=_policy_mapping_fn,  # agent_id → class → policy\r\ncount_steps_by=\"agent_steps\",\r\n)</code></pre></figure>\n<p>Further details are covered in the <strong>\"Problem 2: Loss of Relationship Information Between Entities\"</strong> section.</p>\n<hr class=\"project-divider\" />\n<h4>3. AWS Parallel Training Environment</h4>\n<p>To overcome the scaling limitations of a single local UE5 instance, I developed an <strong>AWS-based distributed training pipeline</strong>. UE5 is packaged as a Linux binary and uploaded to S3, while training scripts are executed via Docker images on EC2 instances to enable high-throughput parallel learning.</p>\n<p>-----</p>\n<h4>Overall Infrastructure Architecture</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/aws_archi.png\" alt=\"Fig 10. AWS Parallel Training Infrastructure Overview\" loading=\"lazy\" /><figcaption>Fig 10. AWS Parallel Training Infrastructure Overview</figcaption></figure>\n<p>-----</p>\n<h4>Infrastructure Components</h4>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Component</th><th>Role</th></tr></thead><tbody><tr><td><strong>Amazon ECR</strong></td><td>Docker image registry for training scripts; pulls UE5 binaries from S3</td></tr><tr><td><strong>EC2 Ray Cluster</strong></td><td>Ray Head + 4 concurrent env\\_runners; performs PPO training via GPU</td></tr><tr><td><strong>Amazon S3</strong></td><td>Persistent storage for UE5 binaries, checkpoints, and training data; integrated with W\\&B</td></tr><tr><td><strong>W\\&B</strong></td><td>Real-time monitoring of rewards, win rates, and loss metrics per iteration</td></tr><tr><td><strong>Terraform</strong></td><td>Provisioning of VPC, IAM roles, and Security Groups</td></tr></tbody></table></div>\n<p>-----</p>\n<h4>Cluster Design Rationale</h4>\n<p>The <strong>Head Node (GPU)</strong> manages policy gradient updates, while <strong>Worker Nodes (CPU Spot)</strong> are dedicated to running headless UE5 instances and collecting rollouts. Since rollout collection is a stateless task that supports interruption and resumption, I utilized <strong>Spot Instances</strong> for workers to optimize cost efficiency.</p>\n<p>-----</p>\n<h4>Training Cycle Summary</h4>\n<ol><li>Build Docker image → Push to ECR</li><li>Initialize Ray cluster (<code>ray up</code>) → Submit training script</li><li>4 Workers collect UE5 rollouts in parallel → Build batch with <code>train_batch_size=4096</code></li><li>PPO gradient updates on Head GPU → Sync checkpoints to S3 every 10 iterations</li><li>Automatically sync to the <code>best/</code> path upon achieving a new record win rate</li></ol>\n<p>Executing <code>ray down</code> terminates all instances immediately, while S3 checkpoints are preserved to ensure seamless training resumption upon restart.</p>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/aws_ec2.png\" alt=\"Fig 11. AWS EC2 Instances\" loading=\"lazy\" /><figcaption>Fig 11. AWS EC2 Instances</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/aws_ps.png\" alt=\"Fig 12. 4 PIDs (UE5 Processes) verified via 'ps aux'\" loading=\"lazy\" /><figcaption>Fig 12. 4 PIDs (UE5 Processes) verified via 'ps aux'</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/aws_s3.png\" alt=\"Fig 13. Amazon S3 Bucket\" loading=\"lazy\" /><figcaption>Fig 13. Amazon S3 Bucket</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/aws_log.png\" alt=\"Fig 14. CloudWatch Logs\" loading=\"lazy\" /><figcaption>Fig 14. CloudWatch Logs</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/wan_spec.png\" alt=\"Fig 15. W\\&amp;B Dashboard Specs\" loading=\"lazy\" /><figcaption>Fig 15. W\\&B Dashboard Specs</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/wan_log.png\" alt=\"Fig 16. W\\&amp;B Training Logs\" loading=\"lazy\" /><figcaption>Fig 16. W\\&B Training Logs</figcaption></figure></div>\n<hr class=\"project-divider\" />\n<h3>Problem Solving</h3>\n<h4>Problem 1: Agent Death Handling Defects in Multi-Agent Reinforcement Learning (Schola + RLlib)</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/problem1.png\" alt=\"Fig 17. Cause and Resolution of the Freezing Phenomenon\" loading=\"lazy\" /><figcaption>Fig 17. Cause and Resolution of the Freezing Phenomenon</figcaption></figure>\n<p><strong>Episode Freeze</strong>: A communication mismatch occurred where RLlib stopped sending actions for an agent that died early, while Unreal Engine (Schola) remained in a wait-state, expecting actions for all agents.</p>\n<p><strong>Death-resurrection loop</strong>: In <code>SAME_STEP</code> mode, deceased agents were immediately reset without a valid state, falling into an infinite loop of repeated deaths.</p>\n<hr class=\"project-divider\" />\n<h4>Cause</h4>\n<p><strong>Conflict between two termination systems</strong></p>\n<p>There was a contradiction in episode termination signals between Schola (<code>AutoResetType::SAME_STEP</code>) and the Python side (RLlib).</p>\n<ul><li><strong>Schola</strong>: Triggered an immediate auto-reset upon agent death per the <code>SAME_STEP</code> policy.</li><li><strong>Python</strong>: RLlib suppressed individual termination signals (<code>done</code>) to prevent mixed trajectories (data from different episodes mixing in a single batch).</li></ul>\n<p>Consequently, the Schola engine would revive the agent only for it to die again instantly, consuming the entire \"step budget.\" Surviving agents were then blocked by Schola's multi-agent step barrier, failing to receive subsequent actions indefinitely.</p>\n<hr class=\"project-divider\" />\n<h4>Goal</h4>\n<p>Establish a stable training environment that operates without interruption even during staggered agent deaths.</p>\n<ul><li>Ensure the entire episode terminates correctly (<code>__all__=True</code>) regardless of individual death timings.</li><li>Implement a filtering system to prevent deceased agents' observations or rewards from contaminating training data (e.g., preventing NaN values).</li></ul>\n<hr class=\"project-divider\" />\n<h4>Solution</h4>\n<p><strong>Dual-layer modifications in Python (Communication) and C++ (Engine)</strong></p>\n<p>Python (Schola Wrapper):</p>\n<ul><li><strong>No-op Padding</strong>: Inserted no-op actions for deceased agents to ensure Unreal always receives actions for the full agent count.</li><li><strong>Data Filtering</strong>: Filtered observations, rewards, and info for already-deceased agents from the Unreal response before passing data to RLlib.</li></ul>\n<p>C++ (Unreal Plugin):</p>\n<ul><li><strong>Dead Agent Snapshot</strong>: Recorded the state of deceased agents before <code>Step()</code> execution and restored terminal flags afterward to prevent state overwriting and resurrection loops.</li><li><strong>Action Filter</strong>: Excluded actions of deceased agents from affecting the physics engine and game logic.</li></ul>\n<hr class=\"project-divider\" />\n<h4>Result</h4>\n<ul><li><strong>Unit Tests Passed</strong>: 100% pass rate across 10 standalone tests (No-op generation, padding protocols, etc.).</li><li><strong>Training Stability</strong>: Resolved episode freezes in integrated Unreal environments and confirmed normal episode reset cycles.</li><li><strong>Data Integrity</strong>: Verified the elimination of NaN values in rewards and prevented trajectory leakage.</li><li><strong>Open Source Contribution</strong>: Submitted a Pull Request to the Schola repository regarding these fixes.</li></ul>\n<figure class=\"project-media\"><img src=\"/images/project1/pr.png\" alt=\"Fig 18. Pull Request\" loading=\"lazy\" /><figcaption>Fig 18. Pull Request</figcaption></figure>\n<hr class=\"project-divider\" />\n<h4>Problem 2: Loss of Relational Information Between Entities</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/problem2.png\" alt=\"Fig 19. Solving Entity Relation Loss through Attention Implementation\" loading=\"lazy\" /><figcaption>Fig 19. Solving Entity Relation Loss through Attention Implementation</figcaption></figure>\n<p>Initially, I attempted to induce cooperation by designing a MAPPO-based team reward structure. However, defining the synergy between three distinct roles—Strike, Support, and Vanguard—as a single scalar reward proved ambiguous. This led to frequent reward hacking without producing meaningful cooperative behavior.</p>\n<p>Shifting away from enforcing cooperation through rewards, I pivoted to an architectural approach that allows agents to naturally learn inter-agent relationships. I identified that the root cause of the agents' inability to recognize spatial patterns—such as 'two enemies gathering at a specific point'—lay in the network structure itself. Specifically, the recurring inefficiency of agents clustering at already captured points was diagnosed as an architectural limitation rather than a flaw in the reward function.</p>\n<hr class=\"project-divider\" />\n<h4>Cause</h4>\n<p>In the 218-dim observation vector, each entity slot is embedded independently via a linear encoder (<code>nn.Linear</code>). While the Self Token subsequently queries the entity set via Cross-Attention, the single-query nature means <strong>relative relationships between entities</strong> (density, flanking patterns, overlap) are not reflected in the Attention weights. The Cross-Attention output is merely a weighted sum of \"how important each entity is to Self,\" lacking information on \"how entities relate to one another.\"</p>\n<hr class=\"project-divider\" />\n<h4>Solution</h4>\n<p><strong>Intra-Set Self-Attention (Zambaldi et al., 2018)</strong></p>\n<p>I inserted a <strong>Self-Attention layer</strong> for each entity group (Ally / Enemy / Point) prior to Cross-Attention, allowing entities to reference each other. Entity tokens processed through Self-Attention now carry contextual information (e.g., \"There are two allies near the same point as me\"). The Self Token then aggregates this <strong>contextualized</strong> entity information during Cross-Attention.</p>\n<figure class=\"project-code-block\"><figcaption>policy.py — Relational Self-Attention pipeline</figcaption><pre><code class=\"language-python\"># 1. Linear Encoding: Raw features → Hidden dimension\r\na_enc = self.ally_enc(allies)                  # (B, 8, 64)\r\n\r\n# 2. Self-Attention: Mutual reference between ally tokens\r\n#    → Learns relations like \"Slot 3 and Slot 5 are near the same point\"\r\na_rel, _ = self.ally_self_attn(a_enc, a_enc, a_enc,\r\n                                key_padding_mask=ally_mask)\r\n\r\n# 3. Residual + LayerNorm: Preserve original info + Stability\r\na_enc = self.ally_ln(a_enc + a_rel)             # (B, 8, 64)\r\n\r\n# 4. Cross-Attention: Self Token aggregates contextualized ally info\r\na_ctx, _ = self.ally_attn(q, a_enc, a_enc,\r\n                          key_padding_mask=ally_mask)  # (B, 1, 64)</code></pre></figure>\n<p><strong>Padding Mask Handling</strong>: The C++ observation layout's <code>0=Valid, 1=Padding</code> mask is applied identically to both Self-Attention and Cross-Attention. A <code>_safe_mask()</code> function forces an unmask of Slot 0 if all slots are padded to prevent NaN. This is completed within the Python policy without requiring C++ modifications.</p>\n<figure class=\"project-code-block\"><figcaption>policy.py — safe mask</figcaption><pre><code class=\"language-python\">def _safe_mask(m: torch.Tensor) -&gt; torch.Tensor:\r\n    all_masked = m.all(dim=1, keepdim=True)   # (B, 1)\r\n    return m &amp; ~all_masked                     # Unmask Slot 0 if all slots are padding</code></pre></figure>\n<p><strong>Design Constraints & Trade-offs</strong></p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Item</th><th>Value</th></tr></thead><tbody><tr><td>Parameter Increase</td><td>168K → 268K (+60%)</td></tr><tr><td>Inference Latency</td><td>< 2ms (0.7% of the 0.3s step budget)</td></tr><tr><td>ONNX Compatibility</td><td>opset 14 — No changes required for UE5 NNE</td></tr><tr><td>C++ Modification</td><td>None (reused padding mask layout)</td></tr></tbody></table></div>\n<p>The empirical results can be found in <strong>\"Results: Empirical Analysis of Attention Patterns\"</strong>.</p>\n<hr class=\"project-divider\" />\n<h4>Problem 3: Timing Mismatch Between Step Speed and Agent Movement</h4>\n<figure class=\"project-media\"><img src=\"/images/project1/problem3.png\" alt=\"Fig 20. Throttling Control\" loading=\"lazy\" /><figcaption>Fig 20. Throttling Control</figcaption></figure>\n<p>In the training environment, <code>AGymConnectorManager::Tick()</code> called <code>Connector-&gt;Step()</code> every frame (60Hz+), causing the next step to execute before EQS-based movement was complete. This caused new EQS goals to overwrite existing ones, canceling movement and resulting in poor-quality training data where observations were collected before reaching targets.</p>\n<hr class=\"project-divider\" />\n<h4>Goal</h4>\n<p>Coordinate the step cycle with the agent's EQS movement completion time to ensure observations are collected only after the agent has reached its destination.</p>\n<hr class=\"project-divider\" />\n<h4>Solution</h4>\n<p><strong>Rationale for overriding <code>AGymConnectorManager</code></strong></p>\n<p>The entry point for Schola's training loop is structured as follows:</p>\n<figure class=\"project-code-block\"><pre><code>AGymConnectorManager::Tick()\n    └─ UAbstractGymConnector::Step()           ← Single training step\n            ├─ ResolveEnvironmentStateUpdate()  ← Receive action from Python (gRPC, ~10ms blocking)\n            ├─ HandleStep() / HandleReset()     ← Apply action to environment\n            └─ SubmitState()                    ← Send observation/reward to Python</code></pre></figure>\n<p><code>UAbstractGymConnector::Step()</code> processes a full cycle atomically, with <code>ResolveEnvironmentStateUpdate()</code> blocking for gRPC responses. Therefore, the <strong>sole point to control step frequency</strong> is <code>AGymConnectorManager::Tick()</code>, which calls <code>Step()</code>. I determined that step speed could be controlled externally by overriding <code>Tick()</code> without modifying the internal connector implementation.</p>\n<hr class=\"project-divider\" />\n<p><strong><code>ADEGymConnectorManager</code> Implementation</strong></p>\n<p>I implemented a custom class <code>ADEGymConnectorManager</code> inheriting from <code>AGymConnectorManager</code>. Instead of the default behavior, I call <code>AActor::Tick()</code> directly and control the frequency of <code>Connector-&gt;Step()</code> using a <code>StepInterval</code> variable.</p>\n<figure class=\"project-code-block\"><figcaption>ADEGymConnectorManager.h</figcaption><pre><code class=\"language-cpp\">// Step interval adjustable in Editor\r\nUPROPERTY(EditAnywhere, Category = \"Schola|Throttling\",\r\n    meta = (ClampMin = \"0.01\", ClampMax = \"10.0\"))\r\nfloat StepInterval = 0.3f;  // Seconds, Default 2Hz</code></pre></figure>\n<p>The <code>StepInterval = 0.3s</code> value was determined by profiling the minimum cycle required: EQS 48-sample query time (~5ms) + NavMesh path calculation (~2ms) + Agent travel time (~1s for 600cm at 600cm/s).</p>\n<figure class=\"project-code-block\"><figcaption>ADEGymConnectorManager.cpp — Implementation of Tick throttling</figcaption><pre><code class=\"language-cpp\">void ADEGymConnectorManager::Tick(float DeltaTime)\r\n{\r\n    // Bypass AGymConnectorManager::Tick to prevent per-frame Step calls\r\n    AActor::Tick(DeltaTime);\r\n\r\n    if (!Connector) return;\r\n\r\n    // Connection Phase: Step internally executes CheckForStart() without blocking\r\n    if (Connector-&gt;IsNotStarted())\r\n    {\r\n        Connector-&gt;Step();\r\n        return;\r\n    }\r\n\r\n    // Running Phase: Execute Step once per StepInterval\r\n    if (Connector-&gt;IsRunning())\r\n    {\r\n        // Prevent burst steps caused by DeltaTime spikes when switching to background\r\n        const float ClampedDelta = FMath::Min(DeltaTime, StepInterval);\r\n        StepAccumulator += ClampedDelta;\r\n        if (StepAccumulator &gt;= StepInterval)\r\n        {\r\n            StepAccumulator = 0.0f;\r\n            Connector-&gt;Step();\r\n        }\r\n    }\r\n}</code></pre></figure>\n<p>Implementation is finalized by changing the parent class of <code>BP_GymConnectorManager</code> to <code>ADEGymConnectorManager</code> in the Editor. The <code>StepInterval</code> can be tuned in the Details panel without requiring a rebuild.</p>\n<hr class=\"project-divider\" />\n<h4>Result</h4>\n<p>With <code>StepInterval = 0.3s</code>, training data quality improved as observations were collected after agents fully reached their EQS targets. Burst steps caused by <code>DeltaTime</code> spikes during editor background transitions were eliminated via <code>FMath::Min</code> clamping. This architecture allows for real-time balancing of training speed and movement completion rates through a single variable.</p>\n<hr class=\"project-divider\" />\n<h3>Results</h3>\n<h4>Training Results</h4>\n<p>Performed MAPPO-based training against Scripted AI over 2.5 million timesteps.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/result_reward.png\" alt=\"Fig 21. Reward\" loading=\"lazy\" /><figcaption>Fig 21. Reward</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/result_vf.png\" alt=\"Fig 22. VF Explained\" loading=\"lazy\" /><figcaption>Fig 22. VF Explained</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/result_entropy.png\" alt=\"Fig 23. Entropy\" loading=\"lazy\" /><figcaption>Fig 23. Entropy</figcaption></figure>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h4>Key Metrics Summary</h4>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Metric</th><th>Value</th><th>Meaning</th></tr></thead><tbody><tr><td><strong>episode_mean</strong></td><td>0 → 40, Converged at 14</td><td>Successful policy improvement</td></tr><tr><td><strong>vf/explained_var</strong></td><td>> 0.8</td><td>Critic explains >80% of future rewards — High state-value prediction accuracy</td></tr><tr><td><strong>entropy</strong></td><td>Initial rise then falling</td><td>Confirmed transition from exploration to exploitation</td></tr></tbody></table></div>\n<p><strong>Termination Rationale:</strong> Training was concluded at 2.5M steps as the learning rate scheduler reached zero and the reward hit a plateau.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<hr class=\"project-divider\" />\n<h4>Win Rate</h4>\n<p>The win rate against Scripted AI increased from an initial 0% to around 70%. The intermediate drop in win rate was a temporary phenomenon caused by the increase in the Scripted AI's tier; as the win rate subsequently recovered, it was confirmed that the RL agent had successfully learned the game rules.</p>\n<figure class=\"project-media\"><img src=\"/images/project1/win_rate.png\" alt=\"Fig 24. win rate\" loading=\"lazy\" /><figcaption>Fig 24. win rate</figcaption></figure>\n<hr class=\"project-divider\" />\n<h4>Miscellaneous Trials & Errors</h4>\n<p><strong>1. Redefining RL Action Space: From Low-level Control to Strategic Positioning</strong></p>\n<p>Initially, I attempted to directly control <strong>low-level actions</strong> such as movement direction, speed, and aiming via Reinforcement Learning. However, local experiments revealed that the number of samples required for convergence was unattainable within a realistic training timeframe.</p>\n<p>To resolve this, I delegated low-level behaviors to Unreal Engine's existing <strong>Navigation and EQS (Environment Query System)</strong> and pivoted the RL agent’s role to focus solely on <strong>strategic positioning</strong>. Specifically, I defined a <strong>7-dimensional weight vector</strong> for EQS queries as the RL policy output. This allowed the policy to decide \"where to be,\" while delegating the execution of movement to the UE5 system, leading to successful convergence.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>2. Self-Play: Policy Collapse in Complex Environments</strong></p>\n<p>Inspired by MuZero, I initially attempted training via <strong>Self-Play</strong>. However, given the high environmental complexity—characterized by heterogeneous roles (<strong>Strike, Support, Vanguard</strong>), fluctuating agent counts, and dynamic objective states—the rewards failed to converge. Instead, the agents suffered from repeated <strong>policy collapse</strong> as both sides attempted to evolve simultaneously.</p>\n<p>I concluded that while Self-Play excels in environments with lower complexity and well-defined action spaces (e.g., Go or Chess), applying it to environments with heterogeneous agents and dynamic goals requires additional stabilization techniques, such as <strong>Curriculum Self-Play</strong> or <strong>League Training</strong>.</p>\n<hr class=\"project-divider\" />\n<h4>Future Plans</h4>\n<p><strong>Implementing Difficulty Tiers for RL Agents</strong></p>\n<ul><li>I plan to experiment with various methods to implement agent difficulty levels (e.g., adjusting total training time vs. applying <strong>Temperature</strong> scaling to the output distribution).</li><li>I will investigate whether to train separate models for each difficulty level or to utilize a single model conditioned on <strong>difficulty-specific parameters</strong>.</li></ul>\n<p><strong>Distributed Training via Multi-EC2 Instances on AWS</strong></p>\n<ul><li>Currently, training is conducted on a single EC2 instance running four UE5 environments simultaneously.</li><li>I aim to establish a <strong>distributed training environment</strong> utilizing multiple EC2 instances and <strong>Ray clusters</strong> to scale the learning process.</li></ul>\n<hr class=\"project-divider\" />\n<h4>Empirical Analysis of Attention Patterns</h4>\n<p>We verified whether the entire pipeline (Self-Attention → Cross-Attention) operates according to the design intent by directly extracting attention weights from the trained checkpoints.</p>\n<hr class=\"project-divider\" />\n<h4>Experimental Design</h4>\n<p>We observed changes in attention distribution by inputting two contrasting scenarios under the same training policy.</p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Scenario</th><th>Description</th></tr></thead><tbody><tr><td><strong>Clustered</strong></td><td>4 allies concentrated within the same base radius (position difference ≈ 0.01)</td></tr><tr><td><strong>Spread</strong></td><td>4 allies distributed across 4 corners of the map (position difference ≈ 0.6)</td></tr></tbody></table></div>\n<p>The attention weights for slots 4–7 were 0.00 across all roles and scenarios, confirming that the padding mask was correctly applied and focus was restricted to valid entities (slots 0–3).</p>\n<hr class=\"project-divider\" />\n<h4>Step 1 — Intra-Set Self-Attention: Capturing Spatial Relationships Between Entities</h4>\n<p>Self-Attention serves as a preprocessing stage for Cross-Attention, where entity tokens reference each other to generate contextualized representations. The key metric is the <strong>fluctuation range of weight distribution (Δmax)</strong> in response to formation changes (Clustered → Spread).</p>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Role</th><th>Formation Sensitivity</th><th>Key Observations</th></tr></thead><tbody><tr><td><strong>Strike</strong></td><td>Medium (Δmax ≈ 0.19)</td><td>Clustered: Even distribution across 4 slots → Spread: Concentrated on Slot 1 (Assault) (0.47)</td></tr><tr><td><strong>Support</strong></td><td>Low (Δmax ≈ 0.06)</td><td>Regardless of formation, maintains even distribution across all slots — Consistent with role characteristics</td></tr><tr><td><strong>Vanguard</strong></td><td>High (Δmax ≈ 0.29)</td><td>Clustered: Concentrated on Slot 3 (0.53) → Spread: Concentrated on Slot 1 (0.54)</td></tr></tbody></table></div>\n<figure class=\"project-media\"><img src=\"/images/project1/attn_comparison_strike.png\" alt=\"Fig 25. STRIKE Self-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 25. STRIKE Self-Attention, Clustered vs Spread vs Difference</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/attn_comparison_support.png\" alt=\"Fig 26. SUPPORT Self-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 26. SUPPORT Self-Attention, Clustered vs Spread vs Difference</figcaption></figure>\n<figure class=\"project-media\"><img src=\"/images/project1/attn_comparison_vanguard.png\" alt=\"Fig 27. VANGUARD Self-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 27. VANGUARD Self-Attention, Clustered vs Spread vs Difference</figcaption></figure>\n<p>The fact that Support shows the lowest sensitivity is not a bug, but a <strong>natural internalization of role characteristics</strong>. The healer role must monitor all allies equally regardless of team formation, and the training reflected this. In contrast, Vanguard showed the highest sensitivity, indicating that the frontline anchor role has differentiated to react most sensitively to changes in team positioning.</p>\n<hr class=\"project-divider\" />\n<h4>Step 2 — Cross-Attention: Final Information Aggregation Before Action</h4>\n<p>In Cross-Attention, the Self Token (one's own observation embedding) acts as the Query to look up each of the three entity sets: Allies, Enemies, and Bases. Since this weighted sum leads directly to EQS weights (actions), it most directly reveals \"what the agent looks at when making actual behavioral decisions.\"</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>Enemy Cross-Attention</strong></p>\n<p>Across all roles, weights are distributed almost equally between active enemy slots 0 and 1 (≈ 0.48–0.52). This is a consistent pattern of perceiving both enemies as equal threats, while slots 2–7 (padding) are accurately suppressed to 0.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>Base Cross-Attention</strong></p>\n<p>Priority bases differ clearly by role. Strike and Support concentrate on bases 0 and 1 (≈ 0.40–0.42), whereas Vanguard shows more distributed weights across bases 1 and 2. This is interpreted as a result of differentiation, where the Vanguard, as a frontline maintenance role, references neutral and rear bases in a balanced manner.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p><strong>Ally Cross-Attention — Consistency Verification with Self-Attention</strong></p>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_strike_clustered.png\" alt=\"Fig 28. STRIKE Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 28. STRIKE Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_strike_spread.png\" alt=\"Fig 29. STRIKE Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 29. STRIKE Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_vanguard_clustered.png\" alt=\"Fig 30. VANGUARD Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 30. VANGUARD Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_vanguard_spread.png\" alt=\"Fig 31. VANGUARD Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 31. VANGUARD Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_support_clustered.png\" alt=\"Fig 32. SUPPORT Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 32. SUPPORT Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project1/attn_cross_support_spread.png\" alt=\"Fig 33. SUPPORT Cross-Attention, Clustered vs Spread vs Difference\" loading=\"lazy\" /><figcaption>Fig 33. SUPPORT Cross-Attention, Clustered vs Spread vs Difference</figcaption></figure></div>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<p>Vanguard's Cross-Attention ally weights correspond exactly with the Self-Attention results. In the Clustered scenario, Self-Attention dominantly selected Slot 3, and Cross-Attention also referenced Slot 3 most highly (0.50). In the Spread scenario, the focus shifted to Slot 1 in both cases (Self: 0.54, Cross: 0.42). This demonstrates that Cross-Attention consistently utilizes the contextualized representations generated by Self-Attention, <strong>confirming that the two-stage pipeline is connected and functioning as intended.</strong></p>\n<hr class=\"project-divider\" />\n<h4>Summary</h4>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Verification Item</th><th>Result</th></tr></thead><tbody><tr><td>Padding Mask Suppression</td><td>Slots 4–7 completely suppressed — Normal</td></tr><tr><td>Role-specific Self-Attention Differentiation</td><td>Support (Equal) / Strike (Medium) / Vanguard (High Sensitivity) — Consistent with role characteristics</td></tr><tr><td>Cross-Attention Entity Priority</td><td>Enemy: Equal threat perception / Base: Varies by role / Ally: Consistent with Self-Attention</td></tr><tr><td>Pipeline Consistency</td><td>Matching focus targets between Self → Cross Attention — Pipeline integrity confirmed</td></tr></tbody></table></div>\n<p>Through the two-stage attention patterns, we empirically demonstrated that <strong>spontaneous differentiation of role-specific spatial reasoning</strong> was achieved using only the Intra-Set Self-Attention structure, without any additional inductive biases such as reward functions or role labels.</p>"
    }
}

export default project1
