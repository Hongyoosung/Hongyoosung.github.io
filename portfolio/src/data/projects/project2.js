const project2 = {
    "id": 2,
    "slug": "gobt",
    "title": {
        "ko": "GOBT: Goal-Oriented Behavior Tree",
        "en": "GOBT: Goal-Oriented Behavior Tree"
    },
    "description": {
        "ko": "Behavior Tree의 구조적 직관성과 GOAP·Utility Theory의 동적 유연성을 결합한 하이브리드 AI 프레임워크",
        "en": "A hybrid decision-making framework combining the intuitiveness of Behavior Trees with the dynamic flexibility of GOAP and Utility Theory."
    },
    "image": "/images/project2/main.png",
    "gif": "/gifs/project2/gobt1.gif",
    "detailImage": "/gifs/project2/gobt2.gif",
    "period": "2023.05 - 2024.02",
    "team": {
        "ko": "2명",
        "en": "2 people"
    },
    "role": {
        "ko": "메인 프로그래머, 논문 작성",
        "en": "Main Programmer, Paper Author"
    },
    "tags": [
        "Behavior Tree",
        "GOAP",
        "Utility AI",
        "Unity",
        "NPC Decision Making"
    ],
    "github": "https://github.com/yoosunghong/GOBTv2.0",
    "paper": "https://doi.org/10.33851/JMIS.2023.10.4.321",
    "highlights": {
        "ko": [
            "표준 Behavior Tree 안에 GOAP와 Utility Theory 기반 커스텀 알고리즘 노드 통합",
            "상위 흐름 제어와 복잡한 상황 판단을 분리한 계층형 AI 구조 설계",
            "Journal of Multimedia Information System 논문으로 연구 결과 출판"
        ],
        "en": [
            "Integrated GOAP and Utility Theory custom algorithm nodes into standard Behavior Trees",
            "Designed a hierarchical AI structure separating flow control from complex situational reasoning",
            "Published the research in the Journal of Multimedia Information System"
        ]
    },
    "content": {
        "ko": "<hr class=\"project-divider\" />\n<h3>개요 (Overview)</h3>\n<p>본 프로젝트는 행동 트리(BT)의 구조적 명확성과 목표 지향 액션 플래닝(GOAP)의 상황 적응성을 융합한 Unity 기반 <strong>계층형 AI 프레임워크</strong> 입니다. 표준 BT 내에 GOAP과 유틸리티 이론을 접목한 커스텀 알고리즘 노드를 통합함으로써, 기존 NPC AI의 정적인 의사결정 한계를 극복하고자 했습니다. 상위 수준의 흐름 제어는 BT가 담당하고, 복잡한 상황 판단은 커스텀 노드에 위임하는 계층적 설계를 통해 연산 효율성과 개발 편의성을 동시에 확보했습니다.</figure></div>\n<div class=\"project-side-by-side\"><figure class=\"project-media\"><img src=\"/images/project2/main.png\" alt=\"Fig 1. 3계층 프레임워크 구조\" loading=\"lazy\" /><figcaption>Fig 1. 3계층 프레임워크 구조</figcaption></figure><div class=\"project-side-content\"><h5>1 계층: Behavior Tree</h5>\n<p>상위 수준의 의사결정(전략)을 담당합니다. 커스텀 노드인 Planner Node를 통해 하위 수준의 의사결정 분기로 진입할 수 있습니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h5>2 계층: Planner Node</h5>\n<p>하위 수준의 의사결정(전술)을 담당합니다. 내부는 상태 그래프로 구성되어 있으며, 각 상태에는 실행할 수 있는 Action이 존재합니다. 그래프에는 시작 상태와 목표 상태가 존재하는데, Planner Node는 목표 상태에 도달하기 위한 Action 체인을 제공합니다.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h5>3 계층: Utility Calculator</h5>\n<p>환경 반응 및 상태 전환을 담당합니다. 상태 전환에 경합이 발생할 경우 현재 게임 환경을 정규화된 유틸리티 수치로 변환하여 경합하는 상태에 대입 후 더 높은 값을 반환하는 상태로 전환합니다.</p></div></div>\n<hr class=\"project-divider\" />\n<h3>기술 스택 (Teck Stak)</h3>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Category</th><th>Technologies</th></tr></thead><tbody><tr><td><strong>Game Engine</strong></td><td>Unity 3D</td></tr><tr><td><strong>AI Architecture</strong></td><td>Behavior Tree, GOAP (Goal-Oriented Action Planning), Utility System</td></tr><tr><td><strong>Tools/Assets</strong></td><td>Behavior Designer</td></tr><tr><td><strong>Language</strong></td><td>C#</td></tr></tbody></table></div>\n<hr class=\"project-divider\" />\n<h3>주요 기능 (Key Features)</h3>\n<h4>1. 플래너 노드를 통한 데이터 주도형 상태 전이</h4>\n<ul><li><strong>Enum 비트마스크 기반 월드 상태 표현</strong>: 에이전트 상태와 환경 데이터를 Enum 기반 비트마스크로 인코딩했습니다. HasWeapon, LowHealth 등의 조건을 비트 플래그로 관리하여, 조건 일치 여부를 문자열 비교나 딕셔너리 조회 없이 빠른 비트 연산(AND/OR)만으로 판별합니다. WorldState 구조체는 불변 값 타입(Immutable Value Type)으로 설계되어 비교 시 힙 할당(Heap Allocation)이 발생하지 않도록 했습니다.</li></ul>\n<figure class=\"project-code-block\"><figcaption>Agnet&World State</figcaption><pre><code class=\"language-csharp\">[Flags]\r\npublic enum WorldStateFlags : uint\r\n{\r\n    None           = 0,\r\n    EnemyVisible   = 1 &lt;&lt; 0,\r\n    EnemyInRange   = 1 &lt;&lt; 1,\r\n    LowHealth      = 1 &lt;&lt; 2,\r\n    HasWeapon      = 1 &lt;&lt; 3,\r\n    IsBlocking     = 1 &lt;&lt; 4,\r\n    EnemyDead      = 1 &lt;&lt; 5,\r\n    InCover        = 1 &lt;&lt; 6,\r\n    IsExhausted    = 1 &lt;&lt; 7,\r\n    HealthRestored = 1 &lt;&lt; 8,\r\n}\r\n\r\n// Immutable value-type wrapper — zero GC allocation during comparisons\r\npublic readonly struct WorldState\r\n{\r\n    public readonly WorldStateFlags Flags;\r\n    public bool Has(WorldStateFlags f)           =&gt; (Flags &amp; f) == f;\r\n    public WorldState With(WorldStateFlags f)    =&gt; new(Flags | f);\r\n    public WorldState Without(WorldStateFlags f) =&gt; new(Flags &amp; ~f);\r\n    public bool Satisfies(WorldStateFlags goal)  =&gt; (Flags &amp; goal) == goal;\r\n    public uint Key =&gt; (uint)Flags; // O(1) key for visited-set / cache lookups\r\n}</code></pre></figure>\n<ul><li><strong>전/후 조건 기반 상태 탐색</strong>: 에이전트의 현재 월드 상태와 다음 가능한 상태의 사전조건(Pre-condition) 마스크를 비교하여 상태를 동적으로 전이합니다. 각 상태에 사전조건과 사후효과(After-effect)만 명시하면 시스템이 목표 도달을 위한 그래프를 자동 생성하므로, 하드코딩된 전이 로직이 필요 없는 데이터 주도형 구조를 실현했습니다.</li></ul>\n<ul><li><strong>유틸리티 기반 상태 평가</strong>: 상태 전이 중 경합이 발생할 경우 유틸리티 평가기에 의해 더 높은 가치를 반환하는 상태로 전이합니다.</li></ul>\n<p><br /></p>\n<h4>2. 유틸리티 기반 실시간 의사결정 최적화</h4>\n<ul><li><strong>다변수 데이터 정규화</strong>: 체력, 거리, 레벨 등 서로 다른 단위의 환경 변수를 <strong>0.0~1.0 범위로 정규화</strong>하여 판단의 객관성을 확보했습니다. 이를 통해 비선형적인 전장 상황을 수치화된 가치로 변환합니다.</li></ul>\n<ul><li><strong>최적 기대 가치 산출</strong>: 단순히 고정된 우선순위를 따르지 않고, 가중치(<span class=\"project-inline-math\">w</span>)가 적용된 유틸리티 함수를 실시간 평가하여 기회비용이 가장 낮고 기대 가치가 높은 행동을 선택합니다.</li></ul>\n<div class=\"project-math\"><img src=\"/formula/project2/utilityformula.svg\" class=\"project-math-svg\" alt=\"Utility Formula\" /></div>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project2/statenode.png\" alt=\"Fig 2. 액션 선택 과정. 에이전트는 선택된 행동의 After-effect를 Current State로 갖는다.\" loading=\"lazy\" /><figcaption>Fig 2. 액션 선택 과정. 에이전트는 선택된 행동의 After-effect를 Current State로 갖는다.</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project2/stategraph.png\" alt=\"Fig 3. State Space Graph\" loading=\"lazy\" /><figcaption>Fig 3. State Space Graph</figcaption></figure></div>\n<figure class=\"project-media\"><img src=\"/images/project2/graph.png\" alt=\"유틸리티 그래프\" loading=\"lazy\" /><figcaption>Fig 4. 정규화와 미세 환경 변화 반응 그래프</figcaption></figure>\n<p><br /></p>\n<h4>3. 모듈화된 Authoring Tool 및 확장성</h4>\n<ul><li><strong>Decoupled Architecture</strong>: 런타임 탐색 로직(Planner)과 개별 행동 로직(Action)을 완전히 분리했습니다. 새로운 행동 추가 시 기존 코드를 수정할 필요 없이 독립적인 액션 노드만 추가하면 되는 <strong>Open-Closed Principle</strong>을 준수합니다.</li></ul>\n<ul><li><strong>ScriptableObject 기반 모듈 조합</strong>: 액션과 유틸리티 함수를 에셋화하여 인스펙터 상에서 액션의 사전/사후 조건을 설정하고 게임 로직과 연결할 수 있습니다.</li></ul>\n<ul><li><strong>인터페이스 기반 확장성 (OCP 준수)</strong>: 플래너 런타임과 개별 액션 로직을 추상 클래스로 엄격히 분리했습니다. 새로운 액션 타입을 추가할 때 플래너 수정 없이 인터페이스만 구현하면 되므로, <strong>개방-폐쇄 원칙(Open-Closed Principle)</strong> 을 충족합니다.</li></ul>\n<figure class=\"project-code-block\"><figcaption>Interface-based extensibility (OCP compliance)</figcaption><pre><code class=\"language-csharp\">// Abstract base — one method contract, zero runtime coupling\r\npublic abstract class GOBTEvaluatorSO : ScriptableObject\r\n{\r\n    /// &lt;returns&gt;Utility score in [0, 1]. 1 = maximally desirable.&lt;/returns&gt;\r\n    public abstract float Evaluate(AgentController agent);\r\n}\r\n\r\n// Example: HealthEvaluatorSO, DistanceEvaluatorSO, ThreatEvaluatorSO,\r\n// and CompositeEvaluatorSO (nestable weighted sum of child evaluators)\r\n// are all authored as .asset files and assigned in the Inspector.</code></pre></figure>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project2/unity1.png\" alt=\"Fig 5. Behavior Designer 내에 배치된 커스텀 플래너 노드\" loading=\"lazy\" /><figcaption>Fig 5. Behavior Designer 내에 배치된 커스텀 플래너 노드</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project2/action.png\" alt=\"Fig 6. 사전조건/사후효과가 정의된 액션 클래스의 인스펙터 창\" loading=\"lazy\" /><figcaption>Fig 6. 사전조건/사후효과가 정의된 액션 클래스의 인스펙터 창</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project2/planner.png\" alt=\"Fig 7. 커스텀 플래너 노드에서 목표 상태와 평가 상태변수를 설정하는 인스펙터 창\" loading=\"lazy\" /><figcaption>Fig 7. 커스텀 플래너 노드에서 목표 상태와 평가 상태변수를 설정하는 인스펙터 창</figcaption></figure></div>\n<hr class=\"project-divider\" />\n<h3>기술적 난제 및 해결 전략 (Problem Solbing)</h3>\n<h4>1. 다중 에이전트 연산 병목 해결 (Time-slicing)</h4>\n<ul><li><strong>Problem</strong>: 수십 명의 에이전트가 동시에 그래프를 구축할 때 발생하는 CPU Spike 현상 확인.</li><li><strong>Solution</strong>: 단일 프레임의 과도한 연산을 방지하기 위해 <strong>코루틴 기반 시분할 처리(Time-slicing)</strong> 기법을 도입. 프레임당 가용 연산 시간을 초과할 경우 작업을 다음 프레임으로 이월.</li><li><strong>Result</strong>: 초기화 시 발생하는 프레임 저하를 <strong>80% 이상 개선</strong>, 대규모 유닛 환경에서도 안정적인 프레임 유지.</li></ul>\n<figure class=\"project-code-block\"><figcaption>Time-slicing</figcaption><pre><code class=\"language-csharp\">public IEnumerator PlanCoroutine(PlanRequest request)\r\n{\r\n    float frameStart = Time.realtimeSinceStartup;\r\n\r\n    while (_openList.Count &gt; 0)\r\n    {\r\n        // Yield to next frame if this frame's budget is exceeded\r\n        if (Time.realtimeSinceStartup - frameStart &gt; FrameBudgetMs * 0.001f)\r\n        {\r\n            yield return null; // resume next frame\r\n            frameStart = Time.realtimeSinceStartup;\r\n        }\r\n\r\n        var current = _openList[0];\r\n        _openList.RemoveAt(0);\r\n\r\n        if (current.State.Satisfies(request.GoalFlags))\r\n        {\r\n            BuildPlan(current, request.ResultPlan);\r\n            FinalizeRequest(request, succeeded: true);\r\n            yield break;\r\n        }\r\n        // ... expand neighbours\r\n    }\r\n}</code></pre></figure>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project2/before.png\" alt=\"Fig 8. 기본 방식(블로킹 발생)\" loading=\"lazy\" /><figcaption>Fig 8. 기본 방식(블로킹 발생)</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project2/after.png\" alt=\"Fig 9. 코루틴 시분할 방식\" loading=\"lazy\" /><figcaption>Fig 9. 코루틴 시분할 방식</figcaption></figure></div>\n<h4>2. 그래프 내 갇힘 문제</h4>\n<ul><li><strong>Problem</strong>: 동적인 환경 변화에 의해 상태 그래프 내에서 목표 상태에 도달하지 못하고 동일한 상태를 순환하여 목표 달성에 실패하는 교착 현상이 확인.</li><li><strong>Solution</strong>: 탐색 알고리즘에 <strong>방문 노드 리스트(Visited List)</strong> 와 최대 탐색 깊이(Max Depth) 제한을 도입했습니다. 또한, 동일 행동 반복 시 유틸리티에 페널티를 부여하는 '스티키니스(Stickiness)' 개념을 적용하여 강제로 다른 대안 노드를 탐색하도록 유도.</li><li><strong>Result</strong>: 예외적인 환경 변화 상황에서도 에이전트가 교착 상태에 빠지지 않고 대안 루트를 탐색하거나 상위 목표로 복귀하는 등 의사결정의 안정성을 확보.</li></ul>\n<figure class=\"project-code-block\"><figcaption>Visited List & Stickiness</figcaption><pre><code class=\"language-csharp\">// Inside PlanCoroutine — Visited List prevents revisiting identical world states\r\nuint key = next.Key;\r\nif (_visited.Contains(key)) continue;\r\n_visited.Add(key);\r\n\r\n// Stickiness: inflate cost when the same action repeats consecutively\r\nfloat cost = action.Cost;\r\nif (current.LastAction != null &amp;&amp; current.LastAction == action)\r\n    cost *= StickinessMultiplier; // default 3×\r\n\r\n// Inside GOBTEvaluationEngine — RepeatPenalty at the evaluation layer\r\nfloat weighted = action.UtilityWeight * Normalize.Clamp01(rawScore);\r\nif (action == _lastAction)\r\n    weighted *= RepeatPenalty; // default 0.55×</code></pre></figure>\n<p><br /></p>\n<h4>3. 유틸리티 계산 비용 최적화 (Weight Caching)</h4>\n<ul><li><strong>Problem</strong>: 상태 전환 시마다 모든 하위 노드의 유틸리티 가중치를 실시간으로 계산하는 방식은 에이전트 수에 비례하여 CPU 연산 부하를 가중.</li><li><strong>Solution</strong>: 유틸리티 값에 영향을 주는 핵심 변수(체력, 거리 등)가 일정 임계값 이상 변하지 않았을 경우, 이전 계산값을 재사용하는 캐싱 매커니즘을 도입했습니다. 또한, 업데이트 주기를 에이전트별로 엇갈리게 배치하는 틱(Tick) 시스템을 병행.</li><li><strong>Result</strong>: 유틸리티 계산 빈도를 효율적으로 조절함으로써, 연산 비용을 최적화하고 더 많은 수의 에이전트를 한 화면에 배치할 수 있는 성능적 여유를 확보.</li></ul>\n<figure class=\"project-code-block\"><figcaption>Weight Caching & Tick System</figcaption><pre><code class=\"language-csharp\">// GOBTEvaluationEngine — cache keyed on the WorldStateFlags bitmask\r\npublic IReadOnlyList&lt;ScoredAction&gt; Score(AgentController agent,\r\n    IReadOnlyList&lt;GOBTActionData&gt; actions, WorldStateFlags currentStateFlags)\r\n{\r\n    // Cache hit: world state unchanged → skip all evaluator calls\r\n    if (currentStateFlags == _cachedStateKey &amp;&amp; _cachedScores.Count &gt; 0)\r\n        return _cachedScores;\r\n\r\n    _cachedStateKey = currentStateFlags;\r\n    _cachedScores.Clear();\r\n    // ... evaluate and sort actions\r\n}\r\n\r\n// GOBTTickManager — only 1/N agents replan per frame (N = TickGroups)\r\nprivate void Update()\r\n{\r\n    int bucket = _frameCount % _tickGroups;\r\n    for (int i = 0; i &lt; _agents.Count; i++)\r\n        if (i % _tickGroups == bucket)\r\n            _agents[i].RequestReplan();\r\n    _frameCount++;\r\n}</code></pre></figure>\n<p><br /></p>\n<h4>4. 오브젝트 풀링을 통한 GC 스파이크 방지</h4>\n<ul><li><strong>Problem</strong>: 상태 그래프 목표 탐색 과정에서 수많은 ActionNode 인스턴스가 생성/소멸되어 빈번한 가비지 컬렉션(GC) 동작 유발.</li></ul>\n<ul><li><strong>Solution</strong>: 시작 시 미리 할당된 <strong>ActionNode Pool</strong> 을 도입. 노드를 렌트하여 사용 후 탐색이 완료되면 일괄 반납하는 구조로 설계하여, 플래닝 시 발생하는 힙 할당을 거의 제로(Zero-allocation)에 가깝게 유지.</li></ul>\n<ul><li><strong>Result</strong>: GC로 인한 프레임 끊김 현상을 제거하여 실시간 전투 시나리오에서의 실용성을 향상.</li></ul>\n<figure class=\"project-code-block\"><figcaption>Object Pooling</figcaption><pre><code class=\"language-csharp\">// Pre-allocated at planner construction — no runtime new() for StateNode\r\nprivate readonly ActionNodePool _nodePool = new(256);\r\n\r\n// Rent a node from the pool; create overflow only if pool is exhausted\r\npublic ActionNode Rent()\r\n{\r\n    if (_pool.Count &gt; 0) return _pool.Pop();\r\n#if UNITY_EDITOR\r\n    Debug.LogWarning(\"[StateNodePool] Pool exhausted — allocating overflow node.\");\r\n#endif\r\n    return new ActionNode();\r\n}\r\n\r\n// After planning: return all rented nodes in one pass, clearing the list\r\npublic void ReturnAll(List&lt;ActionNode&gt; nodes)\r\n{\r\n    foreach (var n in nodes) { n.Reset(); _pool.Push(n); }\r\n    nodes.Clear();\r\n}\r\n\r\n// Usage in PlanCoroutine\r\nvar child = _nodePool.Rent();\r\n_allRented.Add(child);\r\n// ... populate child ...\r\n// After plan resolves:\r\n_nodePool.ReturnAll(_allRented);</code></pre></figure>\n<p><br /></p>\n<hr class=\"project-divider\" />\n<h3>결과 (Results)</h3>\n<ul><li><a href=\"https://doi.org/10.33851/JMIS.2023.10.4.321\" target=\"_blank\" rel=\"noopener noreferrer\"><strong>Journal of Multimedia Information System (JMIS)</strong> 2023년 10월호 4권 'GOBT: A Synergistic Approach to Game AI Using Goal-Oriented and Utility-Based Planning in Behavior Trees'</a> 제 1저자 투고 및 출판 완료.</li><li><strong>역할</strong>: 메인 프로그래머, 논문 작성</li></ul>\n<p><br /></p>",
        "en": "<hr class=\"project-divider\" />\n<h3>Overview</h3>\n<p>This project aims to develop a hierarchical AI framework using the Unity engine that merges the structural clarity of <strong>Behavior Trees (BT)</strong> with the adaptability of <strong>Goal-Oriented Action Planning (GOAP)</strong>. By integrating custom algorithm nodes inspired by GOAP and Utility Theory into a standard BT, I addressed the limitations of static decision-making in NPC AI. The system employs a hierarchical design where high-level flow is managed by the BT, while complex situational reasoning is delegated to custom nodes, achieving both computational efficiency and developer convenience.</figure></div>\n<div class=\"project-side-by-side\"><figure class=\"project-media\"><img src=\"/images/project2/main.png\" alt=\"Figure 1. 3-Layer Framework Structure\" loading=\"lazy\" /><figcaption>Figure 1. 3-Layer Framework Structure</figcaption></figure><div class=\"project-side-content\"><h5>Layer 1: Behavior Tree</h5>\n<p>It is responsible for high-level decision-making (strategy). It can enter lower-level decision branches through the custom node, the Planner Node.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h5>Layer 2: Planner Node</h5>\n<p>It is responsible for low-level decision-making (tactics). Internally, it consists of a state graph, and each state contains an action that can be executed.</p>\n<p>The graph contains a starting state and a target state, and the Planner Node provides a chain of actions to reach the target state.</p>\n<div class=\"project-spacer\" style=\"--space:40px\"></div>\n<h5>Layer 3: Utility Calculator</h5>\n<p>It is responsible for environmental reactions and state transitions. If contention occurs during a state transition, it converts the current game environment into a normalized utility value, applies it to the competing state, and transitions to the state that returns the higher value.</p></div></div>\n<hr class=\"project-divider\" />\n<h3>Tech Stack</h3>\n<div class=\"project-table-wrap\"><table><thead><tr><th>Category</th><th>Technologies</th></tr></thead><tbody><tr><td><strong>Game Engine</strong></td><td>Unity 3D</td></tr><tr><td><strong>AI Architecture</strong></td><td>Behavior Tree, GOAP (Goal-Oriented Action Planning), Utility System</td></tr><tr><td><strong>Tools/Assets</strong></td><td>Behavior Designer</td></tr><tr><td><strong>Language</strong></td><td>C#</td></tr></tbody></table></div>\n<hr class=\"project-divider\" />\n<h3>Key Features</h3>\n<h4>1. Data-Driven State Transitions via Planner Nodes</h4>\n<ul><li><strong>Enum Bitmask-Based World State Representation</strong>: Agent states and environmental data are encoded as <strong>Enum-based Bitmasks</strong>, where each bit flag represents a discrete world condition (e.g., <code>HasWeapon</code>, <code>EnemyInRange</code>, <code>LowHealth</code>). This allows precondition and effect matching to be reduced to fast bitwise <code>AND</code>/<code>OR</code>/<code>XOR</code> operations, eliminating string comparisons and dictionary lookups at runtime. The <code>WorldState</code> struct is an immutable value type, so comparisons produce zero heap allocation.</li></ul>\n<figure class=\"project-code-block\"><figcaption>Agnet&World State</figcaption><pre><code class=\"language-csharp\">[Flags]\r\npublic enum WorldStateFlags : uint\r\n{\r\n    None           = 0,\r\n    EnemyVisible   = 1 &lt;&lt; 0,\r\n    EnemyInRange   = 1 &lt;&lt; 1,\r\n    LowHealth      = 1 &lt;&lt; 2,\r\n    HasWeapon      = 1 &lt;&lt; 3,\r\n    IsBlocking     = 1 &lt;&lt; 4,\r\n    EnemyDead      = 1 &lt;&lt; 5,\r\n    InCover        = 1 &lt;&lt; 6,\r\n    IsExhausted    = 1 &lt;&lt; 7,\r\n    HealthRestored = 1 &lt;&lt; 8,\r\n}\r\n\r\n// Immutable value-type wrapper — zero GC allocation during comparisons\r\npublic readonly struct WorldState\r\n{\r\n    public readonly WorldStateFlags Flags;\r\n    public bool Has(WorldStateFlags f)           =&gt; (Flags &amp; f) == f;\r\n    public WorldState With(WorldStateFlags f)    =&gt; new(Flags | f);\r\n    public WorldState Without(WorldStateFlags f) =&gt; new(Flags &amp; ~f);\r\n    public bool Satisfies(WorldStateFlags goal)  =&gt; (Flags &amp; goal) == goal;\r\n    public uint Key =&gt; (uint)Flags; // O(1) key for visited-set / cache lookups\r\n}</code></pre></figure>\n<ul><li><strong>Pre/Post-condition Based State Search</strong>: The system dynamically transitions states by analyzing the agent's current world state (as a bitmask) against each action's precondition mask. By adopting a <strong>Data-driven</strong> structure—where only <strong>Pre-conditions</strong> and <strong>After-effects</strong> are specified per action—the system automatically constructs the optimal graph to reach the goal without hardcoded transitions.</li><li><strong>Utility-Based Forward Real-time Action Chaining</strong>: To overcome the real-time responsiveness limitations of traditional GOAP (which uses backward planning), I implemented a <strong>Forward Optimization</strong> approach. Starting from the current state, the planner evaluates all immediately applicable actions and chains them greedily by utility score. This allows the system to recalculate weights in real-time based on environmental changes, enabling flexible goal adjustments and action execution.</li></ul>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project2/statenode.png\" alt=\"Figure 2. Action selection process. The agent retains the aftereffect of the selected action as its current state.\" loading=\"lazy\" /><figcaption>Figure 2. Action selection process. The agent retains the aftereffect of the selected action as its current state.</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project2/stategraph.png\" alt=\"Figure 3. State Space Graph\" loading=\"lazy\" /><figcaption>Figure 3. State Space Graph</figcaption></figure></div>\n<h4>2. Utility-Based Real-time Decision Optimization</h4>\n<ul><li><strong>Multi-variable State Normalization & Abstraction</strong>: Battlefield data with different units (HP, Level, Distance, etc.) are <strong>normalized to values between 0.0 and 1.0</strong> to ensure consistency. This transforms non-linear environmental changes into calculable metrics and objectifies the influence of each state on decision-making.</li><li><strong>Optimal Action Selection Mechanism</strong>: Instead of following fixed priorities, the system evaluates utility functions attached to each action node in real-time. Using a weighted (<span class=\"project-inline-math\">w</span>) utility formula, the agent autonomously selects the optimal action with the lowest opportunity cost and highest expected value.</li><li><strong>Context-Aware Responsiveness</strong>: Moving beyond the constraints of fixed FSMs (Finite State Machines), the utility curves react to subtle environmental changes, resulting in organic movements where the agent appears to truly \"understand\" and judge the situation.</li></ul>\n<div class=\"project-math\"><img src=\"/formula/project2/utilityformula.svg\" class=\"project-math-svg\" alt=\"Utility Formula\" /></div>\n<figure class=\"project-media\"><img src=\"/images/project2/graph.png\" alt=\"Utility Graph\" loading=\"lazy\" /><figcaption>Figure 4. Normalization and Environment Response Curves</figcaption></figure>\n<h4>3. Modular Data-Driven Design & Authoring Tool</h4>\n<ul><li><strong>Decoupling State Space Expansion from Evaluation Logic</strong>: While the dynamic state nodes searched by the planner are vast, the number of essential actions (Action Templates) remains limited. I utilized this by connecting evaluation modules to finite Action Templates rather than writing individual logic for every possible state.</li><li><strong>ScriptableObject-Based Composition</strong>: Common utility functions like \"HP Evaluation\" or \"Distance Evaluation\" were modularized into <strong>ScriptableObject</strong> assets (<code>GOBTEvaluatorSO</code>). Designers can simply plug these evaluation assets into action nodes like LEGO blocks within the Inspector.</li></ul>\n<p>```csharp // Abstract base — one method contract, zero runtime coupling public abstract class GOBTEvaluatorSO : ScriptableObject { /// <returns>Utility score in [0, 1]. 1 = maximally desirable.</returns> public abstract float Evaluate(AgentController agent); }</p>\n<p>// Example: HealthEvaluatorSO, DistanceEvaluatorSO, ThreatEvaluatorSO, // and CompositeEvaluatorSO (nestable weighted sum of child evaluators) // are all authored as .asset files and assigned in the Inspector.</p>\n<figure class=\"project-code-block\"><pre><code>\n* **Minimizing Authoring Cost**: This structure allows the system to automatically calculate utilities at runtime by feeding current variables into the assembled assets. Consequently, it drastically reduces **Authoring Cost** and maintenance overhead without artificially limiting the complexity of the state graph.\n* **Interface-Based Extensibility (Open-Closed Principle)**: The planner runtime (`GOBTPlanner`) and individual action logic (`GOBTAction`) are strictly decoupled via abstract base classes. Adding a new action type requires only implementing the interface—the planner requires no modification, fully adhering to the Open-Closed Principle.\n\n\n@@HTML5@@\n\n\n---\n\n## Problem Solving &amp; Optimization\n\n### 1. Asynchronous Graph Construction using Coroutines\n\n* **Problem**: In multi-agent environments, a significant frame drop (spike) occurred when all agents attempted to build their state-space graphs via GPlanner simultaneously at the start.\n* **Solution**: I transitioned from a synchronous approach (processing all nodes in a single frame) to a **Time-slicing** approach based on **Coroutines**. If calculations exceed the allocated time per frame, the process yields to the next frame.\n* **Result**: Improved frame drops during agent initialization by over **80%**, maintaining stable performance even in densely populated environments.\n\n```csharp\npublic IEnumerator PlanCoroutine(PlanRequest request)\n{\n    float frameStart = Time.realtimeSinceStartup;\n\n    while (_openList.Count &gt; 0)\n    {\n        // Yield to next frame if this frame's budget is exceeded\n        if (Time.realtimeSinceStartup - frameStart &gt; FrameBudgetMs * 0.001f)\n        {\n            yield return null; // resume next frame\n            frameStart = Time.realtimeSinceStartup;\n        }\n\n        var current = _openList[0];\n        _openList.RemoveAt(0);\n\n        if (current.State.Satisfies(request.GoalFlags))\n        {\n            BuildPlan(current, request.ResultPlan);\n            FinalizeRequest(request, succeeded: true);\n            yield break;\n        }\n        // ... expand neighbours\n    }\n}</code></pre></figure>\n<div class=\"project-media-grid\"><figure class=\"project-media\"><img src=\"/images/project2/before.png\" alt=\"Figure 8. Basic method (blocking occurs)\" loading=\"lazy\" /><figcaption>Figure 8. Basic method (blocking occurs)</figcaption></figure><figure class=\"project-media\"><img src=\"/images/project2/after.png\" alt=\"Figure 9. Coroutine time-sharing method\" loading=\"lazy\" /><figcaption>Figure 9. Coroutine time-sharing method</figcaption></figure></div>\n<h4>2. Resolving Graph Deadlocks (Oscillation)</h4>\n<ul><li><strong>Problem</strong>: Agents would occasionally fall into loops (oscillation) between specific actions or fail to reach goals due to cycling through identical states in dynamic environments.</li><li><strong>Solution</strong>: Introduced a <strong>Visited List</strong> and <strong>Max Depth</strong> limit to the search algorithm. Furthermore, I implemented a \"<strong>Stickiness</strong>\" concept that applies a weight penalty to repeated actions, forcing the agent to explore alternative nodes. A second <code>RepeatPenalty</code> in the evaluation engine discourages re-selecting the same action on the very next decision cycle.</li><li><strong>Result</strong>: Ensured decision-making stability; agents now successfully navigate alternative routes or return to high-level goals even during exceptional environmental changes.</li></ul>\n<p>```csharp // Inside PlanCoroutine — Visited List prevents revisiting identical world states uint key = next.Key; if (_visited.Contains(key)) continue; _visited.Add(key);</p>\n<p>// Stickiness: inflate cost when the same action repeats consecutively float cost = action.Cost; if (current.LastAction != null && current.LastAction == action) cost *= StickinessMultiplier; // default 3×</p>\n<p>// Inside GOBTEvaluationEngine — RepeatPenalty at the evaluation layer float weighted = action.UtilityWeight * Normalize.Clamp01(rawScore); if (action == _lastAction) weighted *= RepeatPenalty; // default 0.55×</p>\n<figure class=\"project-code-block\"><pre><code>\n&lt;br /&gt;\n\n### 3. Utility Computation Optimization (Weight Caching &amp; Staggered Ticks)\n\n* **Problem**: Real-time recalculation of utility weights for all child nodes during every state transition increased CPU load proportionally to the number of agents.\n* **Solution**: Implemented a **Caching Mechanism** that reuses previous values when the world-state bitmask has not changed — no threshold tuning required, because the bitmask itself is the exact change signal. Additionally, a **Staggered Tick System** (`GOBTTickManager`) offsets replan requests across agents using modulo-based frame bucketing, so not all agents recalculate in the same frame.\n* **Result**: By efficiently regulating computation frequency, I optimized processing costs and gained the performance overhead necessary to support a larger number of on-screen agents.\n\n```csharp\n// GOBTEvaluationEngine — cache keyed on the WorldStateFlags bitmask\npublic IReadOnlyList&lt;ScoredAction&gt; Score(AgentController agent,\n    IReadOnlyList&lt;GOBTActionData&gt; actions, WorldStateFlags currentStateFlags)\n{\n    // Cache hit: world state unchanged → skip all evaluator calls\n    if (currentStateFlags == _cachedStateKey &amp;&amp; _cachedScores.Count &gt; 0)\n        return _cachedScores;\n\n    _cachedStateKey = currentStateFlags;\n    _cachedScores.Clear();\n    // ... evaluate and sort actions\n}\n\n// GOBTTickManager — only 1/N agents replan per frame (N = TickGroups)\nprivate void Update()\n{\n    int bucket = _frameCount % _tickGroups;\n    for (int i = 0; i &lt; _agents.Count; i++)\n        if (i % _tickGroups == bucket)\n            _agents[i].RequestReplan();\n    _frameCount++;\n}</code></pre></figure>\n<p><br /></p>\n<h4>4. GC Spike Prevention via Object Pooling</h4>\n<ul><li><strong>Problem</strong>: During Forward Planning, the planner generates a large number of temporary <code>StateNode</code> objects to represent intermediate world states on the search graph. Under Unity's managed memory model, allocating these with <code>new</code> each frame caused frequent Garbage Collection pauses that scaled badly with agent count.</li><li><strong>Solution</strong>: Introduced a dedicated <strong>Object Pool</strong> (<code>StateNodePool</code>) pre-allocated at startup. Nodes are rented before graph expansion and returned in bulk after the search completes, keeping per-plan heap allocation near zero.</li><li><strong>Result</strong>: Eliminated GC-related frame stutters during plan generation, making the system viable for densely populated combat scenarios without manual GC calls.</li></ul>\n<p>```csharp // Pre-allocated at planner construction — no runtime new() for StateNode private readonly StateNodePool _nodePool = new(256);</p>\n<p>// Rent a node from the pool; create overflow only if pool is exhausted public StateNode Rent() { if (_pool.Count > 0) return _pool.Pop(); #if UNITY_EDITOR Debug.LogWarning(\"[StateNodePool] Pool exhausted — allocating overflow node.\"); #endif return new StateNode(); }</p>\n<p>// After planning: return all rented nodes in one pass, clearing the list public void ReturnAll(List<StateNode> nodes) { foreach (var n in nodes) { n.Reset(); _pool.Push(n); } nodes.Clear(); }</p>\n<p>// Usage in PlanCoroutine var child = _nodePool.Rent(); _allRented.Add(child); // ... populate child ... // After plan resolves: _nodePool.ReturnAll(_allRented);</p>\n<figure class=\"project-code-block\"><pre><code>\n&lt;br /&gt;\n\n---\n\n## Results\n\n* **Publication**: Published as the Lead Author in the **Journal of Multimedia Information System (JMIS)**, Vol. 10, No. 4, October 2023: [\"GOBT: A Synergistic Approach to Game AI Using Goal-Oriented and Utility-Based Planning in Behavior Trees\"](https://doi.org/10.33851/JMIS.2023.10.4.321).\n* **Role**: Lead Programmer, Lead Author\n</code></pre></figure>"
    }
}

export default project2
